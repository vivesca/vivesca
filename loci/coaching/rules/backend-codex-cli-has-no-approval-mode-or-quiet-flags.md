---
title: Codex CLI has no `--approval-mode` or `--quiet` flags
impact: MEDIUM
tags: backend
---

## Codex CLI has no `--approval-mode` or `--quiet` flags

Use `codex exec "<prompt>"` only. Check `codex --help` if unsure about supported flags.
