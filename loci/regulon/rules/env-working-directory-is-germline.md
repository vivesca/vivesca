---
title: Working directory is `~/germline/`
impact: HIGH
tags: env
---

## Working directory is `~/germline/`

(the repo root). `metabolon/`, `effectors/`, `assays/` are all relative to this. Never assume cwd is a subdirectory.
