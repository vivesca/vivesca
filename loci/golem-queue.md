### Pending

- [ ] `golem [t-2e1979] -b zhipu [t-VV10] cd ~/germline. Run: uv run pytest assays/ -q --tb=no --continue-on-collection-errors 2>&1 | grep FAILED | sed 's/::.*//g' | sort | uniq -c | sort -rn | head -5. For the top 3 failing files: run pytest on each, read the error, fix it. Git add and commit each fix with message 'golem: fix <test-file>'. MUST COMMIT.`
- [!] `golem [t-39661e] -b zhipu [t-VV11] cd ~/germline. Read metabolon/organelles/circulation.py. Find all TODO comments. Implement if actionable, remove if stale. Run python3 -c "import ast; ast.parse(open('metabolon/organelles/circulation.py').read()); print('OK')". Git add and commit with 'golem: resolve circulation TODOs'. MUST COMMIT. (retry)`
- [ ] `golem [t-b08088] [t-hardlimit] --provider zhipu --max-turns 40 "Fix billing-cycle rate limit detection in ~/germline/effectors/golem-daemon. READ the full file first — especially the rate-limit detection in the main loop (~line 1500-1550) and _log_cooldown. Bug: when Codex returns 'You've hit your usage limit... try again at Apr 8th, 2026 4:01 PM', the daemon treats it as a normal short cooldown (hours). It should detect billing-cycle exhaustion and set a long cooldown. Changes: (1) In the exit-code/stderr parsing section, detect patterns like 'usage limit', 'hit your.*limit', 'try again at <date>' where date is >24h away. (2) Parse the reset date from stderr (formats: 'Apr 8th, 2026 4:01 PM', ISO, etc). (3) If reset date is >24h away, set cooldown_until to that date and log as 'billing-exhausted' not 'burnout'. (4) Add a PROVIDER_DISABLED dict separate from cooldown — disabled providers are never probed. (5) In status output, show 'codex (billing limit, resets Apr 8)' instead of 'codex (resets 18:36)'. (6) golem-daemon clear-cooldown should still be able to override this. Tests: add tests for billing-limit detection, date parsing from stderr, long vs short cooldown classification. Run: cd ~/germline && uv run pytest assays/test_golem_daemon.py -x -q. Commit with clear message."`
- [x] `golem [t-246f7e] [t-cookiefix] — DROPPED: Mac-only task, can't run on ganglion`
- [x] `golem [t-a869e4] --provider zhipu --max-turns 30 "Modify run_golem() in ~/germline/effectors/golem-daemon (line 1043) to detect and kill rate-limited subprocesses early. Currently it uses subprocess.run (blocking). Change to subprocess.Popen so you can poll stdout periodically. During the first 60s, check accumulated output every 2s against the existing RATE_LIMIT_PATTERNS regex (line 74). If matched, send SIGTERM to the process group (os.killpg), wait 5s, SIGKILL if still alive. Return (cmd, -signal.SIGTERM, tail, duration) — same 4-tuple contract. After 60s stop polling, just proc.wait(). Normal tasks and fast-exit tasks must still work identically. Use start_new_session=True in Popen for process group kill. Test file: assays/test_golem_early_kill.py — run pytest on it, all 6 tests must pass. The 3 kill tests (test_rate_limit_killed_within_10s, test_rate_limit_pattern_AccountQuotaExceeded, test_too_many_requests_pattern) currently hang because the feature doesn't exist yet — your implementation must make them pass in <15s each."`
- [ ] `golem [t-c5c3ee] --provider zhipu --max-turns 30 "Add check_cpu() to ~/germline/effectors/soma-health. READ the full file first. Add a function check_cpu() that reads /proc/loadavg field 1 (1-min load avg), returns Check(name="cpu", status=ok/warn/crit, value=load_str). Thresholds: warn if >6, crit if >12. Wire into run_checks() alongside existing checks so it appears in --json and --daemon output. Test gate: pytest tests/test_soma_health_cpu.py must pass. Do NOT change existing checks. No new files. No new deps."`
- [ ] `golem [t-744a2b] --provider zhipu --max-turns 30 "Add a dedup guard to golem-daemon task dispatch in ~/germline/effectors/golem-daemon. Find where tasks are picked from the queue for execution (the dispatch loop). Before starting a task, extract the prompt/command string (the part after --max-turns N), normalize it (strip task ID, strip whitespace), and check if any currently-running task has the same normalized prompt. If so, skip it (log 'skipping duplicate: [task_id]') and move to the next task. This prevents multiple slots being burned on identical work. Also add dedup on enqueue: in any function that appends to golem-queue.md, check existing pending lines for the same normalized prompt before appending. Test: try to enqueue the same prompt twice — second should be rejected with a message."`
- [ ] `golem [t-timeout1] --provider zhipu --max-turns 20 "Fix timeout alignment in ~/germline/effectors/golem. READ the full file — especially the retry loop (~line 304-310) and the timeout command (~line 374). Bug: golem's internal retry loop (3 retries with exponential backoff starting at 30s) can push total wall-clock time well past the Temporal worker's 30min activity timeout, causing SIGTERM (exit -15) with no output captured. The fix must NOT kill productive work — only cap idle retry/backoff time. Fix: (1) At script start, record _wall_start=$(date +%s). Read GOLEM_WALL_LIMIT env var (default 1680 = 28min). (2) Before each retry's backoff sleep, compute remaining = GOLEM_WALL_LIMIT - (now - _wall_start). If remaining < _claude_timeout (the per-attempt timeout), skip the retry and exit with the last error code. (3) IMPORTANT: do NOT interrupt a claude process that is already running. The wall-limit only gates whether to START a new retry attempt, never kills an in-progress one. The existing per-attempt 'timeout' command handles that. (4) Log 'wall-limit: Nm remaining < Ns per-attempt timeout, skipping retry N' to stderr. Tests: write assays/test_golem_wall_limit.py. Test cases: (1) test_wall_limit_skips_retry — mock a rate-limited first attempt with GOLEM_WALL_LIMIT=5, verify golem exits without retrying and stderr contains 'wall-limit'. (2) test_wall_limit_allows_first_attempt — GOLEM_WALL_LIMIT=9999, verify first attempt runs normally. (3) test_wall_limit_default — unset GOLEM_WALL_LIMIT, verify _wall_start is set and default is 1680. Run: cd ~/germline && uv run pytest assays/test_golem_wall_limit.py -x -q. Commit with clear message."`
- [ ] `golem [t-resume1] --provider zhipu --max-turns 25 "Add partial-progress detection to the Temporal worker retry path in ~/germline/effectors/temporal-golem/worker.py. READ the full file first. Currently when run_golem_task retries (via Temporal RetryPolicy), the second attempt starts from scratch with no knowledge of what the first attempt did. Fix: (1) In run_golem_task, before launching the golem subprocess, run git log --oneline --since=<5min ago> --author=golem in ~/germline to find any commits made by a prior killed attempt. (2) If commits exist, prepend to the task prompt: 'NOTE: A prior attempt on this task made the following commits before being interrupted: <commit list>. Review these commits — if they partially complete the task, continue from where they left off. Do NOT redo already-committed work.' (3) Store the pre-existing HEAD sha before golem runs (already done via _git_snapshot). Use it to scope the log query: git log --oneline pre_head..HEAD would show commits from the current attempt, but for detecting PRIOR attempt work, check git log --oneline --since= with a 40min window matching the activity timeout. (4) Only prepend the note if commits mention the task ID or are within the time window. Do NOT change the activity signature, return type, or any other worker behavior. Tests: write assays/test_temporal_resume.py. Test cases: (1) test_prior_commits_detected — create a dummy 'golem: partial fix' commit within the time window, call the detection function, verify it returns the commit. (2) test_no_prior_commits — clean git state, verify empty result. (3) test_prompt_prepend — given prior commits, verify the augmented prompt contains the NOTE prefix with commit list. (4) test_old_commits_ignored — create a commit outside the time window, verify it's not included. Use a tmp git repo fixture, not the real germline. Run: cd ~/germline && uv run pytest assays/test_temporal_resume.py -x -q. Commit with clear message."`
