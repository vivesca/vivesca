### Pending

- [x] `golem [t-a68919] [t-gather1] --provider zhipu --max-turns 15 "Run cytokinesis gather --syntactic on soma. Print the full JSON output. Then check: (1) any methylation candidates? If yes, list them. (2) any reflect candidates? If yes, list them. (3) stale marks count. (4) MEMORY.md line count vs limit. Write a summary to ~/epigenome/chromatin/immunity/cytokinesis-gather-report.md with timestamp. MUST use: cd ~/germline && cytokinesis gather --syntactic 2>&1. Do NOT modify any files except the report."`
- [x] `golem [t-2e1979] — DROPPED: no test ref, too broad for golem`
- [x] `golem [t-39661e] — DROPPED: no test ref, gate-blocked`
- [x] `golem [t-b08088] — DROPPED: golem-daemon stopped, billing-limit fix not needed`
- [x] `golem [t-246f7e] [t-cookiefix] — DROPPED: Mac-only task, can't run on ganglion`
- [x] `golem [t-a869e4] --provider zhipu --max-turns 30 "Modify run_golem() in ~/germline/effectors/golem-daemon (line 1043) to detect and kill rate-limited subprocesses early. Currently it uses subprocess.run (blocking). Change to subprocess.Popen so you can poll stdout periodically. During the first 60s, check accumulated output every 2s against the existing RATE_LIMIT_PATTERNS regex (line 74). If matched, send SIGTERM to the process group (os.killpg), wait 5s, SIGKILL if still alive. Return (cmd, -signal.SIGTERM, tail, duration) — same 4-tuple contract. After 60s stop polling, just proc.wait(). Normal tasks and fast-exit tasks must still work identically. Use start_new_session=True in Popen for process group kill. Test file: assays/test_golem_early_kill.py — run pytest on it, all 6 tests must pass. The 3 kill tests (test_rate_limit_killed_within_10s, test_rate_limit_pattern_AccountQuotaExceeded, test_too_many_requests_pattern) currently hang because the feature doesn't exist yet — your implementation must make them pass in <15s each."`
- [x] `golem [t-c5c3ee] --provider zhipu --max-turns 30 "Add check_cpu() to ~/germline/effectors/soma-health. READ the full file first. Add a function check_cpu() that reads /proc/loadavg field 1 (1-min load avg), returns Check(name="cpu", status=ok/warn/crit, value=load_str). Thresholds: warn if >6, crit if >12. Wire into run_checks() alongside existing checks so it appears in --json and --daemon output. Test gate: pytest tests/test_soma_health_cpu.py must pass. Do NOT change existing checks. No new files. No new deps."`
- [x] `golem [t-744a2b] --provider zhipu --max-turns 30 "Add a dedup guard to golem-daemon task dispatch in ~/germline/effectors/golem-daemon. Find where tasks are picked from the queue for execution (the dispatch loop). Before starting a task, extract the prompt/command string (the part after --max-turns N), normalize it (strip task ID, strip whitespace), and check if any currently-running task has the same normalized prompt. If so, skip it (log 'skipping duplicate: [task_id]') and move to the next task. This prevents multiple slots being burned on identical work. Also add dedup on enqueue: in any function that appends to golem-queue.md, check existing pending lines for the same normalized prompt before appending. The test file assays/test_golem_dedup.py already exists — run it: cd ~/germline && uv run pytest assays/test_golem_dedup.py -x -q. Commit with clear message. (retry)"`
- [x] `golem [t-be76a0] [t-postgate] --provider zhipu --max-turns 25 "Add a post-golem verification gate to ~/germline/effectors/temporal-golem/worker.py. READ the full file first. After the golem subprocess finishes (line ~150, after post_diff) and ONLY if rc == 0, run three checks before declaring success: (1) ast_check: for each .py file in the git diff --name-only output, run python3 -c 'import ast; ast.parse(open(FILE).read())'. If any fails, set rc=1 and append 'ast_check_failed: FILE' to stderr. (2) test_check: run 'uv run pytest assays/ -q --tb=line -x --timeout=120' from repo root. If exit != 0, set rc=1 and append 'test_regression: <last 10 lines of pytest output>' to stderr. (3) scope_check: extract the target path from the task prompt (first path-like string matching ~/germline/...). Get modified files from git diff --name-only. If any modified file is outside the target dir AND not in assays/, log a warning 'scope_drift: FILE' to stderr (warning only, don't fail). Run all three checks via subprocess with timeout=180 total. If the golem exited non-zero, skip all checks (no point validating broken output). Add the check results to the return dict as 'post_checks': {'ast': bool, 'tests': bool, 'scope_warnings': [...]}. Tests: write assays/test_temporal_post_gate.py with cases: (1) test_ast_check_catches_syntax_error — create a .py file with invalid syntax in tmp repo, verify ast check fails. (2) test_passing_tests_approve — clean state, verify checks pass. (3) test_scope_drift_warns — modify a file outside target, verify warning logged. Run: cd ~/germline && uv run pytest assays/test_temporal_post_gate.py -x -q. Commit with clear message. (retry)"`
- [x] `golem [t-2fea4f] [t-timeout1] — SIGTERM'd by the bug it was fixing. Re-queued as t-timeout2.`
- [x] `golem [t-5c27ea] [t-resume1] — max turns (25). Re-queued as t-resume2 with 40 turns.`
- [x] `golem [t-61b002] [t-timeout2] --provider zhipu --max-turns 10 "Fix timeout alignment in ~/germline/effectors/golem. This is a SMALL bash edit — do not overthink. The file is a bash script. READ lines 295-415 only. Add these changes: (1) Near line 295 (after 'local start_ts'), add: local _wall_start=$(date +%s) and local _wall_limit=${GOLEM_WALL_LIMIT:-1680}. (2) Inside the while loop (line 310), before the backoff sleep (~line 348-351), add a check: local _elapsed=$(( $(date +%s) - _wall_start )) and local _remaining=$(( _wall_limit - _elapsed )). If _remaining < _claude_timeout, then log 'wall-limit: ${_remaining}s remaining < ${_claude_timeout}s per-attempt, skipping retry $_attempt' to stderr and break out of the loop. (3) Same check before the connection-error backoff sleep (~line 361). That is ALL. Do not refactor anything else. The test file assays/test_golem_wall_limit.py already exists — run it: cd ~/germline && uv run pytest assays/test_golem_wall_limit.py -x -q. Commit with message 'golem: add wall-limit to prevent SIGTERM by Temporal'."`
- [!] `golem [t-df1e60] [t-resume2] --provider zhipu --max-turns 40 "Add partial-progress detection to ~/germline/effectors/temporal-golem/worker.py. READ the full file. Add TWO functions after the _git_snapshot function (~line 44): (1) _detect_prior_commits(repo_path: str, since_minutes: int = 40) -> list[str]: runs 'git log --oneline --since=Nminutes.ago' in repo_path, returns list of oneline strings. Filter to only lines containing 'golem' (case-insensitive). Return empty list on error. (2) _build_resume_prompt(task: str, prior_commits: list[str]) -> str: if prior_commits is empty, return task unchanged. Otherwise return 'NOTE: A prior attempt on this task made the following commits before being interrupted:\n' + '\n'.join(prior_commits) + '\nReview these commits — continue from where they left off. Do NOT redo already-committed work.\n\n' + task. Then in run_golem_task (~line 90), before building the cmd list, call: prior = await asyncio.to_thread(_detect_prior_commits, repo_root, 40) and task = _build_resume_prompt(task, prior). The test file assays/test_temporal_resume.py already exists — run it: cd ~/germline && uv run pytest assays/test_temporal_resume.py -x -q. Commit with message 'temporal: add partial-progress detection for retry resilience'. (retry)"`
- [!] `golem [t-c5eef9] [t-pollfix] --provider zhipu --max-turns 15 "Fix temporal dispatch poller stalling in ~/germline/effectors/temporal-golem/dispatch.py. READ the _dispatch_pending function and the poll loop. Bug: after dispatching workflows, the poller blocks for 30+ minutes instead of sleeping 60s and polling again. The issue is likely that the dispatch function awaits workflow completion or the Temporal client call hangs. Fix: (1) In the poll loop, wrap the dispatch call with asyncio.wait_for(timeout=120). If it times out, log '[POLL] dispatch timed out, will retry next cycle' and continue. (2) Ensure workflow start calls are fire-and-forget — dispatch should return as soon as workflows are submitted, not wait for completion. Check if client.start_workflow is used (non-blocking) vs client.execute_workflow (blocking). Do NOT change the dispatch logic itself, only add the timeout wrapper. The test file assays/test_temporal_poll_fix.py already exists — run it: cd ~/germline && uv run pytest assays/test_temporal_poll_fix.py -x -q. Commit with message 'temporal: fix poller stalling after dispatch'. (retry)"`
- [!] `golem [t-ca2248] [t-cachfix] --provider zhipu --max-turns 10 "Fix output cache collision in ~/germline/effectors/temporal-golem/worker.py. READ lines 68-88 (the idempotency cache). Bug: when a task is re-dispatched with the same [t-ID], the worker finds the cached output from the FAILED prior attempt and returns it instantly, skipping execution. The cache was designed for successful idempotency, not failure replay. Fix: in the cache check (~line 76), only return cached result if 'Exit: 0' is in the first 200 chars (success). If the cached file shows a non-zero exit, log 'cache: stale failure for {tid_str}, re-executing' and continue to actual execution. Overwrite the output file at the end so the new result replaces the stale one. Do NOT change the cache key format or output path. The test file assays/test_temporal_cache_fix.py already exists — run it: cd ~/germline && uv run pytest assays/test_temporal_cache_fix.py -x -q. Commit with message 'temporal: skip stale failure cache on re-dispatch'. (retry)"`
- [!] `golem [t-ff66cc] [t-gitsync] --provider zhipu --max-turns 15 "Add automatic git push after golem commits in ~/germline/effectors/temporal-golem/worker.py. READ the full file. After post_diff (~line 150), if rc == 0 and post_diff shows changes (post_diff['stat'] is non-empty), run 'git push' from repo_root with a 30s timeout. If push fails (no credentials, diverged, etc), log the error to stderr but do NOT change rc — the golem work succeeded even if push failed. This keeps ganglion and origin in sync so soma can pull golem output without manual intervention. Also add a pre-golem 'git pull --ff-only' (~line 90, before pre_diff) with a 15s timeout to pick up CC-written test files. If pull fails, log warning and continue — stale code is better than no execution. The test file assays/test_temporal_git_sync.py already exists — run it: cd ~/germline && uv run pytest assays/test_temporal_git_sync.py -x -q. Commit with message 'temporal: auto git sync before/after golem runs'. (retry)"`
