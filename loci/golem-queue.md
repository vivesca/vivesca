# Golem Task Queue

CC writes fully-specified entries. Daemon executes mindlessly. Provider + turns baked in.
**ZhiPu fixed (stdin redirect). All 3 providers active: ZhiPu(4) + Infini(6) + Volcano(8) = 18 concurrent.**

## Pending

### Capco readiness (T-7, perishable)


### Fix operon — test failures (diagnostic first)


### Builds — consulting IP


### Infra — golem reliability




### Mitogen wave 2 — 50 tasks (2026-04-01 afternoon)

#### Consulting IP — deep cards (10 tasks)


#### Fix operon — test failures wave 2 (5 tasks)


<!-- TRIMMED: #### Test coverage — untested metabolon modules (15 tasks) -->
<!-- TRIMMED: #### Test coverage — untested effectors (10 tasks) -->
#### Builds — organism health (5 tasks)


#### Consulting IP — case study shells (5 tasks)


### Mitogen wave 3 -- 50 tasks (2026-04-01 afternoon)

<!-- TRIMMED: #### Test coverage -- metabolon organelles (12 tasks) -->
<!-- TRIMMED: #### Test coverage -- endocytosis subsystem (6 tasks) -->
<!-- TRIMMED: #### Test coverage -- metabolism + resources (8 tasks) -->
<!-- TRIMMED: #### Test coverage -- more effectors (8 tasks) -->
<!-- TRIMMED: #### Test coverage -- sortase + respirometry (4 tasks) -->
#### Hardening -- codebase quality (6 tasks)


#### Builds -- organism tooling (6 tasks)




### Provider resilience (2026-04-01 evening)

#### Quota tracking dashboard




#### Codex provider fix


#### golem-daemon provider concurrency tuning


#### Add codex+gemini to golem-daemon provider tracking




### Mitogen wave 5 -- Capco readiness (2026-04-01 evening)

#### HSBC + regulatory refresh (perishable)


#### HSBC engagement prep refresh


#### Week 1 conversation assets





### CLI setup -- Gemini + Codex parity (2026-04-01)

#### Codex config


#### Gemini config


#### Golem script fixes for non-CC providers



### Provider quality experiment -- same prompt x 4 providers (2026-04-01)



### Mitogen wave 4 -- infra + orchestration (2026-04-01 evening)

#### Fix Hatchet worker (experimental)


#### Fix Temporal setup (experimental)


#### Formalize golem-daemon as primary


#### Retries -- consulting case studies + tooling



### Robustness wave — Fly/Vivesca/Golem (2026-03-31 evening)

#### Fix operon — top 5 failing test files (201 failures → target <50)

#### Fix operon — next 5 failing test files

#### Fix operon — remaining failures (batch smaller files)

#### Fix — collection errors

#### Build — golem-health effector (provider liveness check)

#### Build — daemon log rotation

#### Build — daemon disk space check

#### Build — daemon auto-commit

#### Build — golem stdin safety

#### Build — golem output validator

#### Enhance — golem summary improvements


### Infra wave 2 — Gemmule/Golem/Daemon hardening (2026-03-31 late)

#### Daemon — log rotation (prevents disk fill)

#### Daemon — periodic git commit (don't lose golem output)

#### Daemon — dead task cleanup (purge old [!] entries)

#### Golem — structured JSONL logging (better analytics)

#### Golem — provider fallback (auto-retry on different provider)

#### Gemmule — startup validation script

#### Gemmule — ephemeral file cleanup

#### MCP server — health endpoint

#### Test infra — conftest for platform-aware paths

#### Test infra — ast-check all test files pre-commit hook

#### Monitoring — golem dashboard CLI


### Mega batch (2026-03-31 night) — fixes, health, builds, cleanup



### Browser operon — powerful Playwright-based browser tool

#### Build — cookie sync from Mac Chrome

#### Build — core Playwright browser engine

#### Build — browser MCP tool (vivesca integration)

#### Build — stealth + anti-detection

#### Build — cookie bridge (Mac to gemmule via Tailscale)

#### Build — authenticated browsing integration test

#### Build — browser CLI effector

#### Build — LinkedIn authenticated scraper


### Regulatory circular capture — HSBC-relevant regulators

#### Build — regulatory scraper engine

#### Capture — HKMA circulars (Hong Kong Monetary Authority)

#### Capture — SFC circulars (Securities and Futures Commission HK)

#### Capture — MAS (Monetary Authority of Singapore)

#### Capture — BIS/BCBS (Basel Committee)

#### Capture — PRA/FCA (UK regulators — HSBC HQ jurisdiction)

#### Capture — Fed/OCC (US regulators — HSBC US operations)

#### Capture — ECB/EBA (European regulators)

#### Capture — FATF (anti-money laundering + AI)

#### Capture — FSB (Financial Stability Board)

#### Capture — OWASP (application security — relevant for AI system security)

#### Synthesis — HSBC regulatory landscape report


### Research — Fly.io alternatives for always-on compute




### Build — auto-scale effector + daemon integration





### Build — Oracle Cloud free tier setup

#### Research — Oracle Cloud account + instance provisioning

#### Build — gemmule migration script

#### Build — Tailscale setup automation

#### Build — env/secrets sync tool

#### Build — dual-host health check


### Overnight mega batch (2026-03-31 night)



### Overnight fill — consulting IP + hardening + enhancements



### Post-batch synthesis (run after overnight batch drains)

#### Synthesis — overnight output digest

#### Synthesis — consulting IP executive summary

#### Synthesis — regulatory landscape summary

### Lustro content analysis — process cached articles

#### Build — lustro batch analyzer

#### Process — lustro articles → consulting signals

#### Process — endocytosis content → Capco prep

#### Process — lustro financial articles

#### Process — lustro AI/tech articles

#### Synthesis — weekly intelligence brief


### Meta-golem — self-sustaining review + requeue loop

#### Build — review-and-requeue effector (runs this once, then daemon picks up the cycle)

#### Review cycle 1 (fires ~1h into batch)

#### Review cycle 2 (fires ~2h into batch)

#### Review cycle 3 (fires ~3h into batch)

#### Review cycle 4 (fires ~4h into batch)

#### Review cycle 5 — final synthesis (fires near end of batch)


### Auto-requeue (64 tasks)

### Auto-requeue (19 tasks @ 06:09)

### Auto-requeue (64 tasks)

### Auto-requeue (19 tasks @ 07:29)

### Auto-requeue (19 tasks @ 07:33)

### Auto-requeue (19 tasks @ 07:39)

### Auto-requeue (19 tasks @ 07:44)

### Auto-requeue (19 tasks @ 07:49)

### Auto-requeue (19 tasks @ 07:53)

### Auto-requeue (19 tasks @ 07:57)

### Auto-requeue (19 tasks @ 08:02)

### Auto-requeue (19 tasks @ 08:14)

### Auto-requeue (19 tasks @ 01:16)

### Auto-requeue (19 tasks @ 01:26)

### Auto-requeue (19 tasks @ 01:34)

### Auto-requeue (19 tasks @ 01:44)

### Auto-requeue (19 tasks @ 01:45)

### Auto-requeue (19 tasks @ 01:50)

### Auto-requeue (19 tasks @ 01:52)

### Auto-requeue (19 tasks @ 02:01)

### Auto-requeue (19 tasks @ 02:03)

### Auto-requeue (19 tasks @ 02:14)

### Auto-requeue (19 tasks @ 02:20)

### Auto-requeue (19 tasks @ 02:27)

### Auto-requeue (19 tasks @ 10:36)

### Auto-requeue (19 tasks @ 10:40)

### Auto-requeue (19 tasks @ 02:48)

### Auto-requeue (19 tasks @ 03:02)

### Auto-requeue (19 tasks @ 03:05)

### Auto-requeue (19 tasks @ 03:09)

### Auto-requeue (19 tasks @ 03:15)

### Auto-requeue (19 tasks @ 03:23)

### Auto-requeue (19 tasks @ 03:34)

### Auto-requeue (19 tasks @ 11:40)

### Auto-requeue (19 tasks @ 03:44)

### Auto-requeue (5 tasks @ 12:30)

### Auto-requeue (5 tasks @ 13:30)

### Auto-requeue (5 tasks @ 14:30)

### Auto-requeue (5 tasks @ 15:30)

### Auto-requeue (19 tasks @ 15:42)

### Auto-requeue (19 tasks @ 15:42)

### Auto-requeue (19 tasks @ 15:43)

### Auto-requeue (19 tasks @ 15:43)

### Auto-requeue (19 tasks @ 15:44)

### Auto-requeue (19 tasks @ 15:44)

### Auto-requeue (19 tasks @ 15:45)

### Auto-requeue (19 tasks @ 15:46)

### Auto-requeue (19 tasks @ 15:46)

### Auto-requeue (19 tasks @ 15:47)

### Auto-requeue (19 tasks @ 15:48)

### Auto-requeue (19 tasks @ 15:49)

### Auto-requeue (19 tasks @ 15:50)

### Auto-requeue (19 tasks @ 15:50)

### Auto-requeue (19 tasks @ 15:51)

### Auto-requeue (19 tasks @ 15:52)

### Auto-requeue (19 tasks @ 15:52)

### Auto-requeue (19 tasks @ 15:53)

### Auto-requeue (19 tasks @ 15:54)

### Auto-requeue (19 tasks @ 15:54)

### Auto-requeue (19 tasks @ 15:55)

### Auto-requeue (19 tasks @ 15:56)

### Auto-requeue (19 tasks @ 15:56)

### Auto-requeue (19 tasks @ 15:57)

### Auto-requeue (19 tasks @ 15:58)

### Auto-requeue (19 tasks @ 15:58)

### Auto-requeue (19 tasks @ 15:59)

### Auto-requeue (19 tasks @ 16:00)

### Auto-requeue (19 tasks @ 16:00)

### Auto-requeue (19 tasks @ 16:01)

### Auto-requeue (19 tasks @ 16:02)

### Auto-requeue (19 tasks @ 16:02)

### Auto-requeue (19 tasks @ 16:03)

### Auto-requeue (19 tasks @ 16:04)

### Auto-requeue (19 tasks @ 16:04)

### Auto-requeue (19 tasks @ 16:05)

### Auto-requeue (19 tasks @ 16:06)

### Auto-requeue (19 tasks @ 16:06)

### Auto-requeue (19 tasks @ 16:07)

### Auto-requeue (19 tasks @ 16:08)

### Auto-requeue (19 tasks @ 16:08)

### Auto-requeue (19 tasks @ 16:09)

### Auto-requeue (19 tasks @ 16:10)

### Auto-requeue (19 tasks @ 16:10)

### Auto-requeue (19 tasks @ 16:11)

### Auto-requeue (19 tasks @ 16:12)

### Auto-requeue (19 tasks @ 16:12)

### Auto-requeue (19 tasks @ 16:13)

### Auto-requeue (19 tasks @ 16:15)

### Auto-requeue (19 tasks @ 16:15)

### Auto-requeue (19 tasks @ 16:16)

### Auto-requeue (19 tasks @ 16:17)

### Auto-requeue (19 tasks @ 16:17)

### Auto-requeue (19 tasks @ 16:18)

### Auto-requeue (19 tasks @ 16:19)

### Auto-requeue (19 tasks @ 16:19)

### Auto-requeue (19 tasks @ 16:20)

### Auto-requeue (19 tasks @ 16:21)

### Auto-requeue (19 tasks @ 16:22)

### Auto-requeue (19 tasks @ 16:23)

### Auto-requeue (19 tasks @ 16:23)

### Auto-requeue (19 tasks @ 16:24)

### Auto-requeue (19 tasks @ 16:25)

### Auto-requeue (19 tasks @ 16:25)

### Auto-requeue (19 tasks @ 16:26)

### Auto-requeue (19 tasks @ 16:27)

### Auto-requeue (19 tasks @ 16:27)

### Auto-requeue (19 tasks @ 16:28)

### Auto-requeue (19 tasks @ 16:29)

### Auto-requeue (19 tasks @ 16:29)

### Auto-requeue (19 tasks @ 16:30)

### Auto-requeue (19 tasks @ 16:31)

### Auto-requeue (19 tasks @ 16:31)

### Auto-requeue (19 tasks @ 16:32)

### Auto-requeue (19 tasks @ 16:32)

### Auto-requeue (19 tasks @ 16:33)

### Auto-requeue (19 tasks @ 16:34)

### Auto-requeue (19 tasks @ 16:34)

### Auto-requeue (19 tasks @ 16:35)

### Auto-requeue (19 tasks @ 16:35)

### Auto-requeue (19 tasks @ 16:36)

### Auto-requeue (19 tasks @ 16:37)

### Auto-requeue (19 tasks @ 16:37)

### Auto-requeue (19 tasks @ 16:38)

### Auto-requeue (19 tasks @ 16:39)

### Auto-requeue (19 tasks @ 16:39)

### Auto-requeue (19 tasks @ 16:40)

### Auto-requeue (19 tasks @ 16:41)

### Auto-requeue (19 tasks @ 16:41)

### Auto-requeue (19 tasks @ 16:42)

### Auto-requeue (19 tasks @ 16:43)

### Auto-requeue (19 tasks @ 16:43)

### Auto-requeue (19 tasks @ 16:44)

### Auto-requeue (19 tasks @ 16:45)

### Auto-requeue (19 tasks @ 16:45)

### Auto-requeue (19 tasks @ 18:10)

### Auto-requeue (19 tasks @ 18:11)

### Auto-requeue (19 tasks @ 18:12)

### Auto-requeue (19 tasks @ 18:15)

### Auto-requeue (19 tasks @ 18:58)

### Auto-requeue (19 tasks @ 18:59)

### Auto-requeue (19 tasks @ 19:00)

### Auto-requeue (19 tasks @ 19:01)

### Auto-requeue (19 tasks @ 19:02)

### Auto-requeue (19 tasks @ 19:07)

### Auto-requeue (19 tasks @ 19:08)

### Auto-requeue (19 tasks @ 19:08)

### Auto-requeue (19 tasks @ 19:09)

### Auto-requeue (19 tasks @ 19:12)

### Auto-requeue (19 tasks @ 19:12)

### Auto-requeue (19 tasks @ 19:13)

### Auto-requeue (19 tasks @ 19:14)

### Auto-requeue (19 tasks @ 19:15)

### Auto-requeue (19 tasks @ 19:15)

### Auto-requeue (19 tasks @ 19:16)

### Auto-requeue (19 tasks @ 19:17)

### Auto-requeue (19 tasks @ 19:18)

### Auto-requeue (19 tasks @ 19:19)

### Auto-requeue (19 tasks @ 19:20)
















### Auto-requeue (19 tasks @ 19:20)












































































### Auto-requeue (19 tasks @ 19:25)

### Auto-requeue (19 tasks @ 19:26)

### Auto-requeue (19 tasks @ 19:26)

### Auto-requeue (19 tasks @ 19:27)

### Auto-requeue (19 tasks @ 19:28)

### Auto-requeue (19 tasks @ 19:28)

### Auto-requeue (19 tasks @ 19:30)

### Auto-requeue (19 tasks @ 19:30)

### Auto-requeue (19 tasks @ 19:31)

### Auto-requeue (19 tasks @ 19:32)

### Auto-requeue (19 tasks @ 19:33)

### Auto-requeue (19 tasks @ 19:33)

### Auto-requeue (19 tasks @ 19:34)

### Auto-requeue (19 tasks @ 19:35)

### Auto-requeue (19 tasks @ 19:36)

### Auto-requeue (19 tasks @ 19:37)

### Auto-requeue (19 tasks @ 19:37)

### Auto-requeue (19 tasks @ 19:38)

### Auto-requeue (19 tasks @ 19:38)

### Auto-requeue (19 tasks @ 19:40)

### Auto-requeue (19 tasks @ 19:40)

### Auto-requeue (19 tasks @ 19:41)

### Auto-requeue (19 tasks @ 19:42)

### Auto-requeue (19 tasks @ 19:43)

### Auto-requeue (19 tasks @ 19:44)

### Auto-requeue (19 tasks @ 19:46)

### Auto-requeue (19 tasks @ 19:48)

### Auto-requeue (19 tasks @ 19:49)

### Auto-requeue (19 tasks @ 19:50)

### Auto-requeue (19 tasks @ 19:51)

### Auto-requeue (19 tasks @ 19:51)

### Auto-requeue (19 tasks @ 19:52)

### Auto-requeue (19 tasks @ 19:53)

### Auto-requeue (19 tasks @ 19:54)

### Auto-requeue (19 tasks @ 19:54)

### Auto-requeue (19 tasks @ 19:56)

### Auto-requeue (19 tasks @ 19:56)

### Auto-requeue (19 tasks @ 19:57)

### Auto-requeue (19 tasks @ 19:58)

### Auto-requeue (19 tasks @ 19:59)

### Auto-requeue (19 tasks @ 19:59)

### Auto-requeue (19 tasks @ 20:01)

### Auto-requeue (19 tasks @ 20:01)

### Auto-requeue (19 tasks @ 20:02)

### Auto-requeue (19 tasks @ 20:03)

### Auto-requeue (19 tasks @ 20:04)
- [!] `golem [t-804616] --provider volcano --max-turns 40 "Run uv run pytest --co -q 2>&1 | grep ERROR. Fix ALL collection errors. Common: hardcoded paths, bad imports, syntax. Run --co again until 0 errors. Commit."`
- [!] `golem [t-4181f3] --provider zhipu --max-turns 50 "Run uv run pytest -q --tb=no --continue-on-collection-errors 2>&1 | grep FAILED | sed 's/::.*//g' | sort | uniq -c | sort -rn | head -5. For each of the top 5 failing test files: run pytest on it, read traceback, fix. Iterate until green. Commit."`
- [!] `golem [t-693cb8] --provider infini --max-turns 30 "Health check: rheotaxis, weekly-gather, legatum-verify, generate-solutions-index.py, test-fixer, coverage-map, gap_junction_sync, demethylase, client-brief, commensal. For each: run --help, ast.parse if Python, check shebang. Fix broken ones. Commit."`
- [!] `golem [t-9a285f] --provider volcano --max-turns 30 "Write tests for /home/terry/germline/metabolon/pathways/overnight.py. Mock external calls. Write assays/test_pathways_overnight.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-6ca3f3] --provider zhipu --max-turns 30 "Write tests for /home/terry/germline/metabolon/enzymes/kinesin.py. Mock external calls. Write assays/test_enzymes_kinesin.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-337617] --provider infini --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/golgi.py. Mock external calls. Write assays/test_organelles_golgi.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-2bce09] --provider volcano --max-turns 30 "Write tests for /home/terry/germline/metabolon/enzymes/turgor.py. Mock external calls. Write assays/test_enzymes_turgor.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-589acd] --provider zhipu --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/statolith.py. Mock external calls. Write assays/test_organelles_statolith.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-340fbd] --provider infini --max-turns 30 "Write tests for effectors/start-chrome-debug.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-aa20c7] --provider volcano --max-turns 30 "Write tests for effectors/plan-exec.deprecated. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-93a80d] --provider zhipu --max-turns 30 "Write tests for effectors/tmux-osc52.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-03036c] --provider infini --max-turns 30 "Write tests for effectors/perplexity.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-03ee03] --provider volcano --max-turns 30 "Write tests for effectors/update-compound-engineering-skills.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-b7e69b] --provider zhipu --max-turns 25 "Find test files with SyntaxError. Fix syntax. Commit."`
- [!] `golem [t-cb7849] --provider infini --max-turns 25 "Find subprocess.run calls without timeout in effectors/. Add timeout=300. Commit."`
- [!] `golem [t-97e1c1] --provider volcano --max-turns 25 "Find unused imports in metabolon/**/*.py. Remove them. Commit."`
- [!] `golem [t-66d577] --provider zhipu --max-turns 30 "Write a consulting insight card: Responsible AI governance checklist for financial institutions. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/responsible-ai-checklist.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [!] `golem [t-b7ea4b] --provider infini --max-turns 30 "Write a consulting insight card: AI audit methodology for internal audit teams. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/ai-audit-methodology.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [!] `golem [t-e3072c] --provider volcano --max-turns 35 "Read effectors/golem. Add --json flag that outputs result as JSON instead of raw text. Useful for piping. Write tests. Run uv run pytest. Commit."`

### Auto-requeue (19 tasks @ 20:05)
- [!] `golem [t-f74ec8] --provider zhipu --max-turns 40 "Run uv run pytest --co -q 2>&1 | grep ERROR. Fix ALL collection errors. Common: hardcoded paths, bad imports, syntax. Run --co again until 0 errors. Commit."`
- [!] `golem [t-a1b84d] --provider infini --max-turns 50 "Run uv run pytest -q --tb=no --continue-on-collection-errors 2>&1 | grep FAILED | sed 's/::.*//g' | sort | uniq -c | sort -rn | head -5. For each of the top 5 failing test files: run pytest on it, read traceback, fix. Iterate until green. Commit."`
- [!] `golem [t-9a19ae] --provider volcano --max-turns 30 "Health check: sortase, circadian-probe.py, disk-audit, pharos-env.sh, regulatory-capture, chromatin-backup.py, git-activity, backfill-marks, chromatin-backup.sh, launchagent-health. For each: run --help, ast.parse if Python, check shebang. Fix broken ones. Commit."`
- [!] `golem [t-75ec72] --provider zhipu --max-turns 30 "Write tests for /home/terry/germline/metabolon/pathways/overnight.py. Mock external calls. Write assays/test_pathways_overnight.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-5308f8] --provider infini --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/golgi.py. Mock external calls. Write assays/test_organelles_golgi.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-0e9682] --provider volcano --max-turns 30 "Write tests for /home/terry/germline/metabolon/enzymes/kinesin.py. Mock external calls. Write assays/test_enzymes_kinesin.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-c05c2a] --provider zhipu --max-turns 30 "Write tests for /home/terry/germline/metabolon/enzymes/turgor.py. Mock external calls. Write assays/test_enzymes_turgor.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-2af3e5] --provider infini --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/phenotype_translate.py. Mock external calls. Write assays/test_organelles_phenotype_translate.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-410083] --provider volcano --max-turns 30 "Write tests for effectors/oci-region-subscribe. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-6f08ce] --provider zhipu --max-turns 30 "Write tests for effectors/tmux-url-select.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-4b606a] --provider infini --max-turns 30 "Write tests for effectors/start-chrome-debug.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-8af68c] --provider volcano --max-turns 30 "Write tests for effectors/update-compound-engineering-skills.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-927bc3] --provider zhipu --max-turns 30 "Write tests for effectors/tmux-osc52.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-d06b80] --provider infini --max-turns 25 "Scan effectors/ for hardcoded paths. Fix with Path.home() or $HOME. Commit."`
- [!] `golem [t-08ba52] --provider volcano --max-turns 25 "Find unused imports in metabolon/**/*.py. Remove them. Commit."`
- [!] `golem [t-bf5c93] --provider zhipu --max-turns 25 "Check all assays/test_*.py can be collected by pytest --co. Fix any that error. Commit."`
- [!] `golem [t-4bdc39] --provider infini --max-turns 30 "Write a consulting insight card: AI model risk management framework for banks — write a consulting brief with problem, approach, key considerations. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/ai-model-risk-framework.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [!] `golem [t-da2a67] --provider volcano --max-turns 30 "Write a consulting insight card: LLM deployment risks in banking — regulatory expectations vs reality. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/llm-deployment-risks.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [!] `golem [t-2f7556] --provider zhipu --max-turns 35 "Read effectors/golem-daemon. Add cmd_stats — show pass/fail/retry counts, avg duration by provider, tasks completed today. Usage: golem-daemon stats. Write tests. Run uv run pytest. Commit."`

### Auto-requeue (19 tasks @ 20:06)
- [!] `golem [t-4794f3] --provider infini --max-turns 40 "Run uv run pytest --co -q 2>&1 | grep ERROR. Fix ALL collection errors. Common: hardcoded paths, bad imports, syntax. Run --co again until 0 errors. Commit."`
- [!] `golem [t-535c51] --provider volcano --max-turns 50 "Run uv run pytest -q --tb=no --continue-on-collection-errors 2>&1 | grep FAILED | sed 's/::.*//g' | sort | uniq -c | sort -rn | head -5. For each of the top 5 failing test files: run pytest on it, read traceback, fix. Iterate until green. Commit."`
- [!] `golem [t-2cb22b] --provider zhipu --max-turns 30 "Health check: judge, grok, skill-search, orphan-scan, immunosurveillance.py, generate-solutions-index.py, launchagent-health, oci-region-subscribe, oura-weekly-digest.py, chromatin-backup.sh. For each: run --help, ast.parse if Python, check shebang. Fix broken ones. Commit."`
- [!] `golem [t-2630c4] --provider infini --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/statolith.py. Mock external calls. Write assays/test_organelles_statolith.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-5257aa] --provider volcano --max-turns 30 "Write tests for /home/terry/germline/metabolon/pathways/overnight.py. Mock external calls. Write assays/test_pathways_overnight.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-0ac970] --provider zhipu --max-turns 30 "Write tests for /home/terry/germline/metabolon/enzymes/turgor.py. Mock external calls. Write assays/test_enzymes_turgor.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-43aa8c] --provider infini --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/golgi.py. Mock external calls. Write assays/test_organelles_golgi.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-80cd6f] --provider volcano --max-turns 30 "Write tests for /home/terry/germline/metabolon/enzymes/kinesin.py. Mock external calls. Write assays/test_enzymes_kinesin.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-636618] --provider zhipu --max-turns 30 "Write tests for effectors/pharos-env.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-5d19ad] --provider infini --max-turns 30 "Write tests for effectors/qmd-reindex.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-92cea4] --provider volcano --max-turns 30 "Write tests for effectors/update-compound-engineering-skills.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-1056ac] --provider zhipu --max-turns 30 "Write tests for effectors/tmux-osc52.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-1bee23] --provider infini --max-turns 30 "Write tests for effectors/pharos-health.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-1da681] --provider volcano --max-turns 25 "Scan assays/ for hardcoded /Users/terry paths. Replace with Path.home(). Commit."`
- [!] `golem [t-2ffe4f] --provider zhipu --max-turns 25 "Find test files with SyntaxError. Fix syntax. Commit."`
- [!] `golem [t-f2839e] --provider infini --max-turns 25 "Find subprocess.run calls without timeout in effectors/. Add timeout=300. Commit."`
- [!] `golem [t-d39219] --provider volcano --max-turns 30 "Write a consulting insight card: Board-level AI risk reporting template for banks. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/board-ai-risk-reporting.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [!] `golem [t-f80e0d] --provider zhipu --max-turns 30 "Write a consulting insight card: GenAI policy template for bank employees. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/genai-policy-template.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [!] `golem [t-d438c1] --provider infini --max-turns 35 "Read effectors/golem-review (if exists). Enhance to also check consulting content quality (>200 words, proper structure). Write tests. Run uv run pytest. Commit."`

### Auto-requeue (19 tasks @ 20:08)
- [!] `golem [t-35da68] --provider infini --max-turns 40 "Run uv run pytest --co -q 2>&1 | grep ERROR. Fix ALL collection errors. Common: hardcoded paths, bad imports, syntax. Run --co again until 0 errors. Commit."`
- [!] `golem [t-021d7f] --provider volcano --max-turns 50 "Run uv run pytest -q --tb=no --continue-on-collection-errors 2>&1 | grep FAILED | sed 's/::.*//g' | sort | uniq -c | sort -rn | head -5. For each of the top 5 failing test files: run pytest on it, read traceback, fix. Iterate until green. Commit."`
- [!] `golem [t-fdc699] --provider zhipu --max-turns 30 "Health check: mitosis-checkpoint.py, lustro-analyze, cleanup-stuck, goose-worker, browser, golem-top, immunosurveillance, quorum, cibus.py, test-spec-gen. For each: run --help, ast.parse if Python, check shebang. Fix broken ones. Commit."`
- [!] `golem [t-361bb1] --provider infini --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/golgi.py. Mock external calls. Write assays/test_organelles_golgi.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-752134] --provider volcano --max-turns 30 "Write tests for /home/terry/germline/metabolon/enzymes/turgor.py. Mock external calls. Write assays/test_enzymes_turgor.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-f47ec1] --provider zhipu --max-turns 30 "Write tests for /home/terry/germline/metabolon/pathways/overnight.py. Mock external calls. Write assays/test_pathways_overnight.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-4cfd8c] --provider infini --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/statolith.py. Mock external calls. Write assays/test_organelles_statolith.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-a41b91] --provider volcano --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/phenotype_translate.py. Mock external calls. Write assays/test_organelles_phenotype_translate.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-e7c434] --provider zhipu --max-turns 30 "Write tests for effectors/agent-sync.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-555b59] --provider infini --max-turns 30 "Write tests for effectors/hetzner-bootstrap.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-1875a9] --provider volcano --max-turns 30 "Write tests for effectors/chromatin-backup.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-eca8fb] --provider zhipu --max-turns 30 "Write tests for effectors/tmux-url-select.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-1574cc] --provider infini --max-turns 30 "Write tests for effectors/oci-region-subscribe. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-f02a59] --provider volcano --max-turns 25 "Check all effectors respond to --help without crashing. Fix crashers. Commit."`
- [!] `golem [t-be8bc3] --provider zhipu --max-turns 25 "Scan assays/ for hardcoded /Users/terry paths. Replace with Path.home(). Commit."`
- [!] `golem [t-593ecb] --provider infini --max-turns 25 "Check all assays/test_*.py can be collected by pytest --co. Fix any that error. Commit."`
- [!] `golem [t-2ab248] --provider volcano --max-turns 30 "Write a consulting insight card: AI skills gap assessment for banking technology teams. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/ai-skills-gap.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [!] `golem [t-88f80d] --provider zhipu --max-turns 30 "Write a consulting insight card: Responsible AI governance checklist for financial institutions. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/responsible-ai-checklist.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [!] `golem [t-893709] --provider infini --max-turns 35 "Read effectors/golem-review (if exists). Enhance to also check consulting content quality (>200 words, proper structure). Write tests. Run uv run pytest. Commit."`

### Auto-requeue (19 tasks @ 20:08)
- [!] `golem [t-ce1100] --provider infini --max-turns 40 "Run uv run pytest --co -q 2>&1 | grep ERROR. Fix ALL collection errors. Common: hardcoded paths, bad imports, syntax. Run --co again until 0 errors. Commit."`
- [!] `golem [t-5de702] --provider volcano --max-turns 50 "Run uv run pytest -q --tb=no --continue-on-collection-errors 2>&1 | grep FAILED | sed 's/::.*//g' | sort | uniq -c | sort -rn | head -5. For each of the top 5 failing test files: run pytest on it, read traceback, fix. Iterate until green. Commit."`
- [!] `golem [t-1d6404] --provider zhipu --max-turns 30 "Health check: soma-snapshot, demethylase, coaching-stats, soma-bootstrap, rheotaxis, test-fixer, vesicle, test-spec-gen, update-coding-tools.sh, sortase. For each: run --help, ast.parse if Python, check shebang. Fix broken ones. Commit."`
- [!] `golem [t-ab1f5d] --provider infini --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/statolith.py. Mock external calls. Write assays/test_organelles_statolith.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-88b870] --provider volcano --max-turns 30 "Write tests for /home/terry/germline/metabolon/enzymes/turgor.py. Mock external calls. Write assays/test_enzymes_turgor.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-de56b9] --provider zhipu --max-turns 30 "Write tests for /home/terry/germline/metabolon/pathways/overnight.py. Mock external calls. Write assays/test_pathways_overnight.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-14cc66] --provider infini --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/golgi.py. Mock external calls. Write assays/test_organelles_golgi.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-9a36e9] --provider volcano --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/phenotype_translate.py. Mock external calls. Write assays/test_organelles_phenotype_translate.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-015365] --provider zhipu --max-turns 30 "Write tests for effectors/hetzner-bootstrap.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-79c9ad] --provider infini --max-turns 30 "Write tests for effectors/update-compound-engineering-skills.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-9cc19c] --provider volcano --max-turns 30 "Write tests for effectors/oci-region-subscribe. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-9b2188] --provider zhipu --max-turns 30 "Write tests for effectors/auto-update-compound-engineering.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-a12161] --provider infini --max-turns 30 "Write tests for effectors/tmux-url-select.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-c10db6] --provider volcano --max-turns 25 "Find test files with SyntaxError. Fix syntax. Commit."`
- [!] `golem [t-f7c464] --provider zhipu --max-turns 25 "Check all assays/test_*.py can be collected by pytest --co. Fix any that error. Commit."`
- [!] `golem [t-a3748f] --provider infini --max-turns 25 "Find unused imports in metabolon/**/*.py. Remove them. Commit."`
- [!] `golem [t-a71783] --provider volcano --max-turns 30 "Write a consulting insight card: AI incident response playbook for financial services. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/ai-incident-response.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [!] `golem [t-cad9cf] --provider zhipu --max-turns 30 "Write a consulting insight card: AI audit methodology for internal audit teams. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/ai-audit-methodology.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [!] `golem [t-195834] --provider infini --max-turns 35 "Read effectors/golem-dash (if exists). Improve to show real-time task progress, ETA to drain. Write tests. Run uv run pytest. Commit."`

### Auto-requeue (19 tasks @ 20:09)
- [!] `golem [t-744244] --provider infini --max-turns 40 "Run uv run pytest --co -q 2>&1 | grep ERROR. Fix ALL collection errors. Common: hardcoded paths, bad imports, syntax. Run --co again until 0 errors. Commit."`
- [!] `golem [t-290540] --provider volcano --max-turns 50 "Run uv run pytest -q --tb=no --continue-on-collection-errors 2>&1 | grep FAILED | sed 's/::.*//g' | sort | uniq -c | sort -rn | head -5. For each of the top 5 failing test files: run pytest on it, read traceback, fix. Iterate until green. Commit."`
- [!] `golem [t-9394d9] --provider zhipu --max-turns 30 "Health check: publish, coverage-map, queue-balance, gap_junction_sync, proteostasis, wewe-rss-health.py, cg, linkedin-monitor, diapedesis, complement. For each: run --help, ast.parse if Python, check shebang. Fix broken ones. Commit."`
- [!] `golem [t-ac735c] --provider infini --max-turns 30 "Write tests for /home/terry/germline/metabolon/enzymes/turgor.py. Mock external calls. Write assays/test_enzymes_turgor.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-44220e] --provider volcano --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/golgi.py. Mock external calls. Write assays/test_organelles_golgi.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-02d93b] --provider zhipu --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/statolith.py. Mock external calls. Write assays/test_organelles_statolith.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-1ed9f6] --provider infini --max-turns 30 "Write tests for /home/terry/germline/metabolon/pathways/overnight.py. Mock external calls. Write assays/test_pathways_overnight.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-0c80ba] --provider volcano --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/phenotype_translate.py. Mock external calls. Write assays/test_organelles_phenotype_translate.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-20ec1e] --provider zhipu --max-turns 30 "Write tests for effectors/chromatin-backup.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-b7eda2] --provider infini --max-turns 30 "Write tests for effectors/soma-bootstrap. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-4b9275] --provider volcano --max-turns 30 "Write tests for effectors/tmux-url-select.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-b0cb37] --provider zhipu --max-turns 30 "Write tests for effectors/start-chrome-debug.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-64eb97] --provider infini --max-turns 30 "Write tests for effectors/update-coding-tools.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-cd5e3b] --provider volcano --max-turns 25 "Find Python files missing shebang in effectors/. Add #!/usr/bin/env python3. Commit."`
- [!] `golem [t-d408d6] --provider zhipu --max-turns 25 "Check all assays/test_*.py can be collected by pytest --co. Fix any that error. Commit."`
- [!] `golem [t-d07fc1] --provider infini --max-turns 25 "Scan effectors/ for hardcoded paths. Fix with Path.home() or $HOME. Commit."`
- [!] `golem [t-64dc3b] --provider volcano --max-turns 30 "Write a consulting insight card: GenAI policy template for bank employees. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/genai-policy-template.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [!] `golem [t-6586f1] --provider zhipu --max-turns 30 "Write a consulting insight card: Responsible AI governance checklist for financial institutions. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/responsible-ai-checklist.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [!] `golem [t-e78fae] --provider infini --max-turns 35 "Read effectors/golem. Add --json flag that outputs result as JSON instead of raw text. Useful for piping. Write tests. Run uv run pytest. Commit."`

### Auto-requeue (19 tasks @ 20:10)
- [!] `golem [t-d330d8] --provider zhipu --max-turns 40 "Run uv run pytest --co -q 2>&1 | grep ERROR. Fix ALL collection errors. Common: hardcoded paths, bad imports, syntax. Run --co again until 0 errors. Commit."`
- [!] `golem [t-989f0a] --provider infini --max-turns 50 "Run uv run pytest -q --tb=no --continue-on-collection-errors 2>&1 | grep FAILED | sed 's/::.*//g' | sort | uniq -c | sort -rn | head -5. For each of the top 5 failing test files: run pytest on it, read traceback, fix. Iterate until green. Commit."`
- [!] `golem [t-067114] --provider volcano --max-turns 30 "Health check: diapedesis, lacuna, client-brief, fix-symlinks, golem-health, skill-sync, golem, disk-audit, browse, golem-review. For each: run --help, ast.parse if Python, check shebang. Fix broken ones. Commit."`
- [!] `golem [t-428150] --provider zhipu --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/golgi.py. Mock external calls. Write assays/test_organelles_golgi.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-0e2156] --provider infini --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/statolith.py. Mock external calls. Write assays/test_organelles_statolith.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-1b6a3b] --provider volcano --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/phenotype_translate.py. Mock external calls. Write assays/test_organelles_phenotype_translate.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-3fccd1] --provider zhipu --max-turns 30 "Write tests for /home/terry/germline/metabolon/pathways/overnight.py. Mock external calls. Write assays/test_pathways_overnight.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-29ade1] --provider infini --max-turns 30 "Write tests for /home/terry/germline/metabolon/enzymes/turgor.py. Mock external calls. Write assays/test_enzymes_turgor.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-251a7a] --provider volcano --max-turns 30 "Write tests for effectors/pharos-health.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-ce8599] --provider zhipu --max-turns 30 "Write tests for effectors/update-compound-engineering-skills.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-e86b9a] --provider infini --max-turns 30 "Write tests for effectors/pharos-sync.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-b43b65] --provider volcano --max-turns 30 "Write tests for effectors/oci-region-subscribe. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-61f517] --provider zhipu --max-turns 30 "Write tests for effectors/chromatin-backup.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-033db4] --provider infini --max-turns 25 "Scan effectors/ for hardcoded paths. Fix with Path.home() or $HOME. Commit."`
- [!] `golem [t-49d198] --provider volcano --max-turns 25 "Scan assays/ for hardcoded /Users/terry paths. Replace with Path.home(). Commit."`
- [!] `golem [t-af37db] --provider zhipu --max-turns 25 "Check all effectors respond to --help without crashing. Fix crashers. Commit."`
- [!] `golem [t-405d4e] --provider infini --max-turns 30 "Write a consulting insight card: AI bias testing framework for credit decisioning. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/ai-bias-testing.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [!] `golem [t-77b42c] --provider volcano --max-turns 30 "Write a consulting insight card: AI incident response playbook for financial services. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/ai-incident-response.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [!] `golem [t-54590f] --provider zhipu --max-turns 35 "Read effectors/golem-review (if exists). Enhance to also check consulting content quality (>200 words, proper structure). Write tests. Run uv run pytest. Commit."`

### Auto-requeue (19 tasks @ 20:11)
- [!] `golem [t-e2f7df] --provider zhipu --max-turns 40 "Run uv run pytest --co -q 2>&1 | grep ERROR. Fix ALL collection errors. Common: hardcoded paths, bad imports, syntax. Run --co again until 0 errors. Commit."`
- [!] `golem [t-e19f9e] --provider infini --max-turns 50 "Run uv run pytest -q --tb=no --continue-on-collection-errors 2>&1 | grep FAILED | sed 's/::.*//g' | sort | uniq -c | sort -rn | head -5. For each of the top 5 failing test files: run pytest on it, read traceback, fix. Iterate until green. Commit."`
- [!] `golem [t-84c0ef] --provider volcano --max-turns 30 "Health check: circadian-probe.py, proteostasis, poiesis, oura-weekly-digest.py, find, provider-bench, test-dashboard, rename-plists, wewe-rss-health.py, golem-daemon-wrapper.sh. For each: run --help, ast.parse if Python, check shebang. Fix broken ones. Commit."`
- [!] `golem [t-81c766] --provider zhipu --max-turns 30 "Write tests for /home/terry/germline/metabolon/enzymes/turgor.py. Mock external calls. Write assays/test_enzymes_turgor.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-08984f] --provider infini --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/golgi.py. Mock external calls. Write assays/test_organelles_golgi.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-125804] --provider volcano --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/statolith.py. Mock external calls. Write assays/test_organelles_statolith.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-460f67] --provider zhipu --max-turns 30 "Write tests for /home/terry/germline/metabolon/pathways/overnight.py. Mock external calls. Write assays/test_pathways_overnight.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-a19573] --provider infini --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/phenotype_translate.py. Mock external calls. Write assays/test_organelles_phenotype_translate.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-7a4eba] --provider volcano --max-turns 30 "Write tests for effectors/pharos-env.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-04ebfc] --provider zhipu --max-turns 30 "Write tests for effectors/start-chrome-debug.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-32af80] --provider infini --max-turns 30 "Write tests for effectors/plan-exec.deprecated. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-fae190] --provider volcano --max-turns 30 "Write tests for effectors/golem-daemon-wrapper.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-340ff5] --provider zhipu --max-turns 30 "Write tests for effectors/tmux-osc52.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-0cfe8f] --provider infini --max-turns 25 "Find unused imports in metabolon/**/*.py. Remove them. Commit."`
- [!] `golem [t-574ff0] --provider volcano --max-turns 25 "Scan assays/ for hardcoded /Users/terry paths. Replace with Path.home(). Commit."`
- [!] `golem [t-66cce6] --provider zhipu --max-turns 25 "Find test files with SyntaxError. Fix syntax. Commit."`
- [!] `golem [t-b8038c] --provider infini --max-turns 30 "Write a consulting insight card: AI skills gap assessment for banking technology teams. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/ai-skills-gap.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [!] `golem [t-2e8a5b] --provider volcano --max-turns 30 "Write a consulting insight card: Data governance for AI/ML training data in banking. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/data-governance-ai.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [!] `golem [t-706c8e] --provider zhipu --max-turns 35 "Read effectors/golem. Add --json flag that outputs result as JSON instead of raw text. Useful for piping. Write tests. Run uv run pytest. Commit."`

### Auto-requeue (19 tasks @ 20:12)
- [!] `golem [t-f445e7] --provider infini --max-turns 40 "Run uv run pytest --co -q 2>&1 | grep ERROR. Fix ALL collection errors. Common: hardcoded paths, bad imports, syntax. Run --co again until 0 errors. Commit."`
- [!] `golem [t-34d708] --provider volcano --max-turns 50 "Run uv run pytest -q --tb=no --continue-on-collection-errors 2>&1 | grep FAILED | sed 's/::.*//g' | sort | uniq -c | sort -rn | head -5. For each of the top 5 failing test files: run pytest on it, read traceback, fix. Iterate until green. Commit."`
- [!] `golem [t-850a00] --provider zhipu --max-turns 30 "Health check: update-compound-engineering, legatum, compound-engineering-test, oci-arm-retry, browser, methylation, search-guard, dr-sync, conftest-gen, oci-region-subscribe. For each: run --help, ast.parse if Python, check shebang. Fix broken ones. Commit."`
- [!] `golem [t-a01d17] --provider infini --max-turns 30 "Write tests for /home/terry/germline/metabolon/pathways/overnight.py. Mock external calls. Write assays/test_pathways_overnight.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-db8862] --provider volcano --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/phenotype_translate.py. Mock external calls. Write assays/test_organelles_phenotype_translate.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-e6edc0] --provider zhipu --max-turns 30 "Write tests for /home/terry/germline/metabolon/enzymes/turgor.py. Mock external calls. Write assays/test_enzymes_turgor.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-a46423] --provider infini --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/golgi.py. Mock external calls. Write assays/test_organelles_golgi.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-680f00] --provider volcano --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/statolith.py. Mock external calls. Write assays/test_organelles_statolith.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-9e78ea] --provider zhipu --max-turns 30 "Write tests for effectors/soma-bootstrap. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-9df30c] --provider infini --max-turns 30 "Write tests for effectors/chromatin-backup.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-129359] --provider volcano --max-turns 30 "Write tests for effectors/oci-region-subscribe. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-7b76c6] --provider zhipu --max-turns 30 "Write tests for effectors/agent-sync.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-020538] --provider infini --max-turns 30 "Write tests for effectors/backup-due.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-7a178f] --provider volcano --max-turns 25 "Scan effectors/ for hardcoded paths. Fix with Path.home() or $HOME. Commit."`
- [!] `golem [t-94fae3] --provider zhipu --max-turns 25 "Check all assays/test_*.py can be collected by pytest --co. Fix any that error. Commit."`
- [!] `golem [t-52f35e] --provider infini --max-turns 25 "Find Python files missing shebang in effectors/. Add #!/usr/bin/env python3. Commit."`
- [!] `golem [t-8b000d] --provider volcano --max-turns 30 "Write a consulting insight card: AI bias testing framework for credit decisioning. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/ai-bias-testing.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [!] `golem [t-d9180c] --provider zhipu --max-turns 30 "Write a consulting insight card: GenAI policy template for bank employees. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/genai-policy-template.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [!] `golem [t-5999d0] --provider infini --max-turns 35 "Read effectors/golem-dash (if exists). Improve to show real-time task progress, ETA to drain. Write tests. Run uv run pytest. Commit."`

### Auto-requeue (18 tasks @ 20:13)
- [!] `golem [t-06449f] --provider volcano --max-turns 40 "Run uv run pytest --co -q 2>&1 | grep ERROR. Fix ALL collection errors. Common: hardcoded paths, bad imports, syntax. Run --co again until 0 errors. Commit."`
- [!] `golem [t-f0e1df] --provider zhipu --max-turns 50 "Run uv run pytest -q --tb=no --continue-on-collection-errors 2>&1 | grep FAILED | sed 's/::.*//g' | sort | uniq -c | sort -rn | head -5. For each of the top 5 failing test files: run pytest on it, read traceback, fix. Iterate until green. Commit."`
- [!] `golem [t-d89538] --provider infini --max-turns 30 "Health check: cg, centrosome, goose-worker, test-fixer, soma-watchdog, nightly, express, log-summary, git-activity, golem-health. For each: run --help, ast.parse if Python, check shebang. Fix broken ones. Commit."`
- [!] `golem [t-ef1339] --provider volcano --max-turns 30 "Write tests for /home/terry/germline/metabolon/enzymes/turgor.py. Mock external calls. Write assays/test_enzymes_turgor.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-0f3864] --provider zhipu --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/golgi.py. Mock external calls. Write assays/test_organelles_golgi.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-f14fe4] --provider infini --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/phenotype_translate.py. Mock external calls. Write assays/test_organelles_phenotype_translate.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-ca50d0] --provider volcano --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/statolith.py. Mock external calls. Write assays/test_organelles_statolith.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-8f555f] --provider zhipu --max-turns 30 "Write tests for effectors/backup-due.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-4004d7] --provider infini --max-turns 30 "Write tests for effectors/perplexity.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-1bcfa1] --provider volcano --max-turns 30 "Write tests for effectors/auto-update-compound-engineering.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-5e66cb] --provider zhipu --max-turns 30 "Write tests for effectors/golem-daemon-wrapper.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-59bf99] --provider infini --max-turns 30 "Write tests for effectors/chromatin-backup.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-4549de] --provider volcano --max-turns 25 "Find subprocess.run calls without timeout in effectors/. Add timeout=300. Commit."`
- [!] `golem [t-467f71] --provider zhipu --max-turns 25 "Find test files with SyntaxError. Fix syntax. Commit."`
- [!] `golem [t-da63e9] --provider infini --max-turns 25 "Scan effectors/ for hardcoded paths. Fix with Path.home() or $HOME. Commit."`
- [!] `golem [t-90e79d] --provider volcano --max-turns 30 "Write a consulting insight card: LLM deployment risks in banking — regulatory expectations vs reality. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/llm-deployment-risks.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [!] `golem [t-2b4747] --provider zhipu --max-turns 30 "Write a consulting insight card: AI incident response playbook for financial services. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/ai-incident-response.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [!] `golem [t-72fcf4] --provider infini --max-turns 35 "Read effectors/golem. Add --json flag that outputs result as JSON instead of raw text. Useful for piping. Write tests. Run uv run pytest. Commit."`

### Auto-requeue (18 tasks @ 20:14)
- [!] `golem [t-d53744] --provider zhipu --max-turns 40 "Run uv run pytest --co -q 2>&1 | grep ERROR. Fix ALL collection errors. Common: hardcoded paths, bad imports, syntax. Run --co again until 0 errors. Commit."`
- [!] `golem [t-aec621] --provider infini --max-turns 50 "Run uv run pytest -q --tb=no --continue-on-collection-errors 2>&1 | grep FAILED | sed 's/::.*//g' | sort | uniq -c | sort -rn | head -5. For each of the top 5 failing test files: run pytest on it, read traceback, fix. Iterate until green. Commit."`
- [!] `golem [t-f14f36] --provider volcano --max-turns 30 "Health check: council, centrosome, rename-plists, queue-balance, queue-stats, soma-activate, safe_rm.py, test-spec-gen, oura-weekly-digest.py, evident-brief. For each: run --help, ast.parse if Python, check shebang. Fix broken ones. Commit."`
- [!] `golem [t-cbea34] --provider zhipu --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/statolith.py. Mock external calls. Write assays/test_organelles_statolith.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-0a49ed] --provider infini --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/phenotype_translate.py. Mock external calls. Write assays/test_organelles_phenotype_translate.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-83312e] --provider volcano --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/golgi.py. Mock external calls. Write assays/test_organelles_golgi.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-e39bc2] --provider zhipu --max-turns 30 "Write tests for /home/terry/germline/metabolon/enzymes/turgor.py. Mock external calls. Write assays/test_enzymes_turgor.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-4297b1] --provider infini --max-turns 30 "Write tests for effectors/qmd-reindex.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-08c475] --provider volcano --max-turns 30 "Write tests for effectors/oci-region-subscribe. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-760171] --provider zhipu --max-turns 30 "Write tests for effectors/chromatin-backup.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-b04a7c] --provider infini --max-turns 30 "Write tests for effectors/plan-exec.deprecated. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-7752c7] --provider volcano --max-turns 30 "Write tests for effectors/pharos-health.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-a1e066] --provider zhipu --max-turns 25 "Find Python files missing shebang in effectors/. Add #!/usr/bin/env python3. Commit."`
- [!] `golem [t-4e2a75] --provider infini --max-turns 25 "Find unused imports in metabolon/**/*.py. Remove them. Commit."`
- [!] `golem [t-673d37] --provider volcano --max-turns 25 "Find test files with SyntaxError. Fix syntax. Commit."`
- [!] `golem [t-a32d8d] --provider zhipu --max-turns 30 "Write a consulting insight card: Responsible AI governance checklist for financial institutions. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/responsible-ai-checklist.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [!] `golem [t-efff6d] --provider infini --max-turns 30 "Write a consulting insight card: GenAI policy template for bank employees. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/genai-policy-template.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [!] `golem [t-454350] --provider volcano --max-turns 35 "Read effectors/golem-review (if exists). Enhance to also check consulting content quality (>200 words, proper structure). Write tests. Run uv run pytest. Commit."`

### Auto-requeue (18 tasks @ 20:14)
- [!] `golem [t-2a0e38] --provider volcano --max-turns 40 "Run uv run pytest --co -q 2>&1 | grep ERROR. Fix ALL collection errors. Common: hardcoded paths, bad imports, syntax. Run --co again until 0 errors. Commit."`
- [!] `golem [t-2941c9] --provider zhipu --max-turns 50 "Run uv run pytest -q --tb=no --continue-on-collection-errors 2>&1 | grep FAILED | sed 's/::.*//g' | sort | uniq -c | sort -rn | head -5. For each of the top 5 failing test files: run pytest on it, read traceback, fix. Iterate until green. Commit."`
- [!] `golem [t-146ef8] --provider infini --max-turns 30 "Health check: legatum-verify, mitosis-checkpoint.py, rotate-logs.py, immunosurveillance, queue-balance, tmux-workspace.py, disk-audit, coverage-map, orphan-scan, paracrine. For each: run --help, ast.parse if Python, check shebang. Fix broken ones. Commit."`
- [!] `golem [t-0b69ca] --provider volcano --max-turns 30 "Write tests for /home/terry/germline/metabolon/enzymes/turgor.py. Mock external calls. Write assays/test_enzymes_turgor.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-cf62b4] --provider zhipu --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/statolith.py. Mock external calls. Write assays/test_organelles_statolith.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-fa4ad8] --provider infini --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/golgi.py. Mock external calls. Write assays/test_organelles_golgi.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-fbe6d2] --provider volcano --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/phenotype_translate.py. Mock external calls. Write assays/test_organelles_phenotype_translate.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-da0a2d] --provider zhipu --max-turns 30 "Write tests for effectors/agent-sync.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-15286b] --provider infini --max-turns 30 "Write tests for effectors/evident-brief. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-98ddd4] --provider volcano --max-turns 30 "Write tests for effectors/auto-update-compound-engineering.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-2d9f12] --provider zhipu --max-turns 30 "Write tests for effectors/update-compound-engineering-skills.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-bcb3e8] --provider infini --max-turns 30 "Write tests for effectors/pharos-sync.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-e7b8d6] --provider volcano --max-turns 25 "Find test files with SyntaxError. Fix syntax. Commit."`
- [!] `golem [t-1d8189] --provider zhipu --max-turns 25 "Scan assays/ for hardcoded /Users/terry paths. Replace with Path.home(). Commit."`
- [!] `golem [t-32866f] --provider infini --max-turns 25 "Check all assays/test_*.py can be collected by pytest --co. Fix any that error. Commit."`
- [!] `golem [t-4dae8a] --provider volcano --max-turns 30 "Write a consulting insight card: AI audit methodology for internal audit teams. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/ai-audit-methodology.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [!] `golem [t-180597] --provider zhipu --max-turns 30 "Write a consulting insight card: AI incident response playbook for financial services. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/ai-incident-response.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [!] `golem [t-858eff] --provider infini --max-turns 35 "Read effectors/golem-review (if exists). Enhance to also check consulting content quality (>200 words, proper structure). Write tests. Run uv run pytest. Commit."`

### Auto-requeue (18 tasks @ 20:15)
- [!] `golem [t-9cc11c] --provider volcano --max-turns 40 "Run uv run pytest --co -q 2>&1 | grep ERROR. Fix ALL collection errors. Common: hardcoded paths, bad imports, syntax. Run --co again until 0 errors. Commit."`
- [!] `golem [t-7774b4] --provider zhipu --max-turns 50 "Run uv run pytest -q --tb=no --continue-on-collection-errors 2>&1 | grep FAILED | sed 's/::.*//g' | sort | uniq -c | sort -rn | head -5. For each of the top 5 failing test files: run pytest on it, read traceback, fix. Iterate until green. Commit."`
- [!] `golem [t-5e43b6] --provider infini --max-turns 30 "Health check: mitosis-checkpoint.py, queue-balance, golem-reviewer, channel, qmd-reindex.sh, soma-pull, respirometry, skill-sync, efferens, tmux-url-select.sh. For each: run --help, ast.parse if Python, check shebang. Fix broken ones. Commit."`
- [!] `golem [t-62acff] --provider volcano --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/golgi.py. Mock external calls. Write assays/test_organelles_golgi.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-4c8a9c] --provider zhipu --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/statolith.py. Mock external calls. Write assays/test_organelles_statolith.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-ad4fed] --provider infini --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/phenotype_translate.py. Mock external calls. Write assays/test_organelles_phenotype_translate.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-b20e83] --provider volcano --max-turns 30 "Write tests for /home/terry/germline/metabolon/enzymes/turgor.py. Mock external calls. Write assays/test_enzymes_turgor.py. Run uv run pytest. Fix failures. Commit."`
- [!] `golem [t-90396f] --provider zhipu --max-turns 30 "Write tests for effectors/auto-update-compound-engineering.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [ ] `golem [t-a66cd8] --provider infini --max-turns 30 "Write tests for effectors/pharos-env.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [ ] `golem [t-04cc84] --provider volcano --max-turns 30 "Write tests for effectors/golem-daemon-wrapper.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [ ] `golem [t-56ee60] --provider zhipu --max-turns 30 "Write tests for effectors/start-chrome-debug.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [ ] `golem [t-0ae462] --provider infini --max-turns 30 "Write tests for effectors/plan-exec.deprecated. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [ ] `golem [t-e031c5] --provider volcano --max-turns 25 "Find subprocess.run calls without timeout in effectors/. Add timeout=300. Commit."`
- [!] `golem [t-a86c5a] --provider zhipu --max-turns 25 "Check all assays/test_*.py can be collected by pytest --co. Fix any that error. Commit."`
- [ ] `golem [t-6ddfa1] --provider infini --max-turns 25 "Scan assays/ for hardcoded /Users/terry paths. Replace with Path.home(). Commit."`
- [ ] `golem [t-d148f4] --provider volcano --max-turns 30 "Write a consulting insight card: LLM deployment risks in banking — regulatory expectations vs reality. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/llm-deployment-risks.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [ ] `golem [t-c59a72] --provider zhipu --max-turns 30 "Write a consulting insight card: GenAI policy template for bank employees. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/genai-policy-template.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [ ] `golem [t-54392b] --provider infini --max-turns 35 "Read effectors/golem. Add --json flag that outputs result as JSON instead of raw text. Useful for piping. Write tests. Run uv run pytest. Commit."`

### Auto-requeue (18 tasks @ 20:16)
- [ ] `golem [t-750c0f] --provider zhipu --max-turns 40 "Run uv run pytest --co -q 2>&1 | grep ERROR. Fix ALL collection errors. Common: hardcoded paths, bad imports, syntax. Run --co again until 0 errors. Commit."`
- [ ] `golem [t-19310d] --provider infini --max-turns 50 "Run uv run pytest -q --tb=no --continue-on-collection-errors 2>&1 | grep FAILED | sed 's/::.*//g' | sort | uniq -c | sort -rn | head -5. For each of the top 5 failing test files: run pytest on it, read traceback, fix. Iterate until green. Commit."`
- [ ] `golem [t-493ce6] --provider volcano --max-turns 30 "Health check: cg, golem-health, synthase, gap_junction_sync, chromatin-backup.py, golem-validate, git-activity, receptor-scan, publish, qmd-reindex.sh. For each: run --help, ast.parse if Python, check shebang. Fix broken ones. Commit."`
- [ ] `golem [t-c6b0f0] --provider zhipu --max-turns 30 "Write tests for /home/terry/germline/metabolon/enzymes/turgor.py. Mock external calls. Write assays/test_enzymes_turgor.py. Run uv run pytest. Fix failures. Commit."`
- [ ] `golem [t-23ec42] --provider infini --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/statolith.py. Mock external calls. Write assays/test_organelles_statolith.py. Run uv run pytest. Fix failures. Commit."`
- [ ] `golem [t-6c8ff8] --provider volcano --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/golgi.py. Mock external calls. Write assays/test_organelles_golgi.py. Run uv run pytest. Fix failures. Commit."`
- [ ] `golem [t-fafbdf] --provider zhipu --max-turns 30 "Write tests for /home/terry/germline/metabolon/organelles/phenotype_translate.py. Mock external calls. Write assays/test_organelles_phenotype_translate.py. Run uv run pytest. Fix failures. Commit."`
- [ ] `golem [t-5ca2fc] --provider infini --max-turns 30 "Write tests for effectors/hetzner-bootstrap.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [ ] `golem [t-dd1927] --provider volcano --max-turns 30 "Write tests for effectors/pharos-env.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [ ] `golem [t-aa5a0d] --provider zhipu --max-turns 30 "Write tests for effectors/start-chrome-debug.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [ ] `golem [t-6455f0] --provider infini --max-turns 30 "Write tests for effectors/tmux-osc52.sh. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [ ] `golem [t-9a456e] --provider volcano --max-turns 30 "Write tests for effectors/evident-brief. Effectors are scripts — use exec or subprocess.run. NEVER import. Run uv run pytest. Fix failures. Commit."`
- [ ] `golem [t-770912] --provider zhipu --max-turns 25 "Scan assays/ for hardcoded /Users/terry paths. Replace with Path.home(). Commit."`
- [ ] `golem [t-a1410a] --provider infini --max-turns 25 "Find unused imports in metabolon/**/*.py. Remove them. Commit."`
- [ ] `golem [t-d3ec7e] --provider volcano --max-turns 25 "Find subprocess.run calls without timeout in effectors/. Add timeout=300. Commit."`
- [ ] `golem [t-641ee5] --provider zhipu --max-turns 30 "Write a consulting insight card: Responsible AI governance checklist for financial institutions. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/responsible-ai-checklist.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [ ] `golem [t-e28cdc] --provider infini --max-turns 30 "Write a consulting insight card: LLM deployment risks in banking — regulatory expectations vs reality. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/llm-deployment-risks.md. Structure: problem (2 sentences), why it matters (3 bullets), approach (numbered steps), considerations, Capco angle. 500-800 words."`
- [ ] `golem [t-d7f4c4] --provider volcano --max-turns 35 "Read effectors/golem-review (if exists). Enhance to also check consulting content quality (>200 words, proper structure). Write tests. Run uv run pytest. Commit."`
## Done (2026-03-31)


### Build — circadian-aware auto-requeue


- [ ] `golem [t-35fcf3] --provider codex --max-turns 30 "Create ~/germline/loci/priorities.md with this structure: north_stars \(3 items with deadline\), current_focus \(what to prioritize now\), blocked \(what to skip\). Initial content: \(1\) Capco readiness — deadline Apr 8 — consulting IP, regulatory briefs, case studies \(2\) Organism robustness — ongoing — fix tests, effector health \(3\) Consulting arsenal — ongoing — frameworks, templates. CC will update this file each session."`


### Build — wire circulation into golem-daemon




### High-priority — infra + task IDs (queued by CC)

#### Add task IDs to golem-daemon

#### Fix all from __future__ duplicate bugs

#### Fix collection errors blocking test suite

#### Fix top 10 failing tests

### Consulting IP — Capco prep (T-7)

#### AI risk management consulting cards

#### HK regulatory landscape briefing
- [ ] `golem [t-211b2c] --provider codex --max-turns 40 "Write a comprehensive briefing on Hong Kong financial regulation for AI/technology: HKMA, SFC, IA key circulars and expectations around AI adoption in banking, insurance, securities. Include HKMA's Supervisory Policy Manual modules on technology risk \(TM-E-1, OR-1\). Write to ~/epigenome/chromatin/euchromatin/consulting/cards/hk-ai-regulatory-landscape.md. 500+ words. Commit." (retry)`

### System hardening

#### Add periodic rsync from Mac to gemmule

#### Scan and fix hardcoded macOS paths
- [ ] `golem [t-f031c4] --provider codex --max-turns 30 "Find ALL files in ~/germline/ containing /Users/terry. Replace with Path.home\(\) \(Python\) or $HOME \(shell\). Verify nothing breaks. Commit." (retry)`

### Research — workflow orchestration for golem-daemon

#### Landscape: Temporal vs alternatives for AI task orchestration

### Resilience — vivesca self-healing and hardening

#### Daemon auto-restart via supervisor
- [ ] `golem [t-fd4826] --provider codex --max-turns 30 "Read the supervisor config for golem-daemon. If none exists, check /etc/supervisor/conf.d/. Create or fix a supervisor config at /etc/supervisor/conf.d/golem-daemon.conf that: 1\) runs 'python3 /home/terry/germline/effectors/golem-daemon start --foreground' as user terry 2\) auto-restarts on crash 3\) sets environment from /home/terry/.env.fly \(source it in a wrapper script if needed\) 4\) logs stdout/stderr to /home/terry/.local/share/vivesca/. Write tests. Commit." (retry)`

#### Git auto-backup — push epigenome to remote

#### Provider failover in golem

#### Watchdog — detect and fix stuck golems

#### Graceful shutdown — commit before dying

#### Config validation on daemon start

#### Self-healing — auto-fix common breakages

#### Network resilience — retry git operations
- [ ] `golem [t-9dffa6] --provider codex --max-turns 30 "Read effectors/golem-daemon, specifically auto_commit\(\). Add retry logic: if git push fails \(network error\), retry 3 times with 30s backoff. If all retries fail, log error but don't crash daemon. Also add: if git pull fails at startup, continue with local state rather than crashing. Write tests. Commit." (retry)`

#### Backup marks/memory to git
- [ ] `golem [t-a6dcae] --provider codex --max-turns 30 "The epigenome/marks directory contains critical behavioral memory files. Ensure they are tracked in git: cd ~/epigenome && git add marks/ && git status. If there are untracked marks, commit them. Also create a pre-push hook or effector that validates all marks have valid YAML frontmatter. Write tests. Commit."`

### Temporal migration — Phase 1

#### Scaffold Temporal-based golem orchestrator

#### Docker compose for Temporal server
- [ ] `golem [t-74f0cc] --provider codex --max-turns 30 "Create effectors/temporal-golem/docker-compose.yml for running Temporal locally. Include: 1\) temporal-server \(temporalio/server:latest\) 2\) PostgreSQL 15 for persistence 3\) temporal-web \(temporalio/web:latest\) on port 8080 4\) temporal-admin-tools for CLI access. Use environment variables for config. Add a startup script effectors/temporal-golem/start.sh that does 'docker compose up -d' and waits for health check. Write to effectors/temporal-golem/. Commit." (retry)`

### Provider troubleshooting

#### Diagnose and fix Volcano provider failures

#### Diagnose and fix Infini provider failures  
- [ ] `golem [t-f1adb3] --provider codex --max-turns 40 "Read effectors/golem \(the shell script\). Infini provider \(deepseek-v3.2 at cloud.infini-ai.com\) has intermittent exit=2 failures with 0s duration. Debug: 1\) Check INFINI_API_KEY is correctly formatted 2\) Test the endpoint directly: curl -s https://cloud.infini-ai.com/maas/coding/v1/chat/completions -H 'Authorization: Bearer $INFINI_API_KEY' -H 'Content-Type: application/json' -d '{\"model\":\"deepseek-v3.2\",\"messages\":[{\"role\":\"user\",\"content\":\"hi\"}]}' 3\) Check if rate limiting or quota exhaustion is the cause 4\) Read daemon logs for infini-specific error patterns. If rate limited, implement per-provider cooldown in golem-daemon. Write findings and fix. Commit." (retry)`

### Hatchet features — wire up advanced capabilities

#### Rate limits — per-provider server-side throttling

#### Cron — scheduled auto-requeue and health checks

#### Metrics — task stats and Prometheus endpoint

#### Event-driven dispatch — git push triggers task pickup

#### Durable tasks — survive worker restarts

#### Logs — centralized task logging

---

### Python rewrites of Rust CLIs (eliminate cargo from Docker build)

All tools: single Python file at ~/bin/<name>, #!/usr/bin/env python3, use requests or urllib, argparse with subcommands matching the Rust CLI exactly. chmod +x after creation. Test each command. Use env vars for API keys (no hardcoded secrets).

#### Tier 1 — daily use




- [ ] `golem [t-816e83] --provider codex --max-turns 30 "Build ~/bin/stips as a Python CLI for OpenRouter credits. Env: OPENROUTER_API_KEY. Subcommands: credits \(GET https://openrouter.ai/api/v1/auth/key — print balance\), usage \(GET https://openrouter.ai/api/v1/activity — print recent usage\), key \(print masked key\). Use requests. Test with: stips credits. chmod +x."`

#### Tier 2 — weekly use

- [ ] `golem [t-0a3b79] --provider codex --max-turns 30 "Build ~/bin/fasti as a Python CLI wrapping Google Calendar API. Env: GOOGLE_API_KEY. Subcommands: list [date] \(list events for a date, default today\), move <event-id> <new-datetime>, delete <event-id>. Use the Google Calendar REST API with API key auth. For OAuth operations that need write access, use a service account or stored refresh token at ~/.config/fasti/credentials.json. Print events as: time | title | location. Test with: fasti list. chmod +x."`



- [ ] `golem [t-b1e7c2] --provider codex --max-turns 30 "Build ~/bin/sarcio as a Python CLI for managing a digital garden. Posts live at ~/notes/Writing/Blog/Published/. Subcommands: new <title> \(create a new draft .md file with frontmatter: title, date, tags, status=draft\), list \(list all posts with status\), publish <filename> \(set status=published in frontmatter, add published_date\), revise <filename> \(open in EDITOR\), open <filename> \(open in EDITOR\), index \(regenerate index.md listing all published posts\). Use pathlib and yaml \(PyYAML or frontmatter parsing\). Test with: sarcio list. chmod +x."`

#### Tier 3 — specialized


- [ ] `golem [t-6e540c] --provider codex --max-turns 30 "Build ~/bin/moneo as a Python CLI for Due app reminders. Due stores data in ~/Library/Group Containers/ on Mac but on Linux we use a synced JSON file at ~/.config/moneo/reminders.json. Subcommands: ls \(list all reminders sorted by date\), add <title> --date <datetime> [--repeat <interval>] \(add reminder\), rm <title> \(delete by title match\), edit <index> --title/--date \(edit fields\), log \(show completion history from ~/.config/moneo/completions.jsonl\). Print reminders as: date | title | repeat. Test with: moneo ls \(should show empty or create sample data\). chmod +x."`



### Hatchet dogfooding — advanced features

#### Webhooks — trigger from GitHub push
- [ ] `golem [t-013aa4] --provider codex --max-turns 40 "Read Hatchet webhooks API. Create a webhook endpoint that GitHub can call on push to vivesca repo. When triggered, dispatch new pending tasks from golem-queue.md. Steps: 1\) Use h.webhooks.create\(\) to register a webhook 2\) Create effectors/hatchet-golem/webhook.py that handles the GitHub payload 3\) Update .github/workflows/gemmule-wake.yml to also POST to the Hatchet webhook after waking gemmule. This replaces polling — tasks dispatch on push. Write tests. Commit."`

#### Worker labels + sticky sessions — provider affinity

#### Priority queuing — high-priority tasks first

#### Listener — real-time task completion stream

#### Backoff — exponential retry for rate limits

#### Filters — route tasks by content

#### Multi-worker scaling — separate workers per provider

#### Prometheus + Grafana observability

### Consulting IP — Hatchet vs Temporal for banking clients

#### Hatchet vs Temporal comparison card for financial services

#### AI agent orchestration patterns for enterprise

#### Hatchet quick-start guide for technical audience

#### Build vs buy for AI orchestration — honest consulting card

### Resilience & DR (organism survival)

#### DR runbook: iMac as warm standby for soma
Write a disaster recovery runbook at ~/epigenome/chromatin/System/soma-dr-runbook.md. Scenario: Fly.io dies or soma is unreachable for >1hr. The iMac (m3, Tailscale hostname m3) becomes primary. Cover: 1) Pre-requisites already on iMac (supervisor, repos, API keys in Keychain) 2) What needs to be synced: germline, epigenome, .claude config, .env equivalents 3) A single script `soma-failover` that: pulls latest from GitHub for germline+epigenome, starts supervisor with local soma.conf equivalent, starts Hatchet worker, verifies MCP server 4) What DOESN'T work on iMac (Docker golems, some Linux-only tools) and workarounds 5) Failback procedure once Fly recovers. Keep it under 200 lines. Commit.

#### Build soma-failover script
Write ~/germline/effectors/soma-failover as a Python script. When run on iMac: 1) git pull germline + epigenome 2) Check/start supervisor with macOS-adapted config 3) Start hatchet-worker 4) Start vivesca MCP 5) Run integrin health check 6) Report status. Should be idempotent — safe to run multiple times. Commit.

#### Build soma-health-check endpoint
Add a /health endpoint to vivesca MCP server that returns JSON: {status, uptime, supervisor_procs, disk_free_pct, last_golem_run, git_status}. This becomes the liveness probe — if soma stops responding, iMac failover triggers. Check if vivesca already has a health route first. Commit.

#### Automated soma backup to iMac
Write ~/germline/effectors/soma-backup as a Python script that rsyncs critical state from soma to iMac via Tailscale: ~/.claude/, ~/.env.fly, ~/tmp/supervisord.log, ~/epigenome/ (if not git-pushed). Run as a cron every 6 hours. Skip if iMac is offline (Tailscale status check). Commit.

#### Multi-cloud readiness: Oracle free tier bootstrap
Write a bootstrap script ~/germline/effectors/soma-oracle-bootstrap that provisions a free Oracle Cloud ARM instance as a cold standby. Similar to hetzner-bootstrap.sh but for Oracle free tier (4 OCPU, 24GB RAM ARM). Install same stack as soma Dockerfile. Don't auto-provision — just the script ready to run. Commit.

#### Supervisor config portability
Make soma.conf work on both Linux (Fly) and macOS (iMac). Currently hardcoded to /home/terry paths. Write a script ~/germline/effectors/soma-supervisor-gen that generates the right supervisord.conf for the current OS (detects Linux vs macOS, adjusts paths, skips Docker-only services on macOS). Commit.

#### Hatchet self-hosted resilience
The self-hosted Hatchet runs in Docker on soma. If Docker dies, all golem dispatch stops. Write ~/germline/effectors/hatchet-recover that: 1) Checks if Hatchet containers are running 2) If not, docker compose up -d 3) Waits for health 4) Restarts hatchet-worker supervisor process 5) Replays any tasks that were in-flight. Check docker-compose location first. Commit.

#### Golem provider failover chain
Review ~/germline/effectors/golem and document the current failover logic. Then enhance: if primary provider fails 3x, auto-rotate to next available provider (zhipu→gemini→codex→infini→volcano). Currently fallback exists but may not cover all providers. Add codex to the fallback chain. Commit.

#### Volume snapshot automation
Write ~/germline/effectors/soma-snapshot that creates a Fly volume snapshot via `fly volumes snapshots create`. Run weekly as a cron. Needs FLY_API_TOKEN. Check if fly CLI is installed first. Commit.

#### Network partition handling
What happens if Tailscale goes down? Document in ~/epigenome/chromatin/System/network-partition-playbook.md: 1) Direct SSH via fly ssh console 2) fly proxy for port forwarding 3) Direct IP access if allocated 4) What breaks without Tailscale (iMac↔soma, inter-service mesh). Keep under 100 lines. Commit.

### High-value batch (lunch run, 2026-04-01 12:20)

### Mitogen wave 3 — evening batch (2026-04-01 ~19:30)

#### Fix operon — diagnose and fix E-block (5 tasks)
Concentrated runtime errors around 8-11% of test suite. Likely common root cause across golem-generated tests.


#### Fix operon — failing tests wave 3 (5 tasks)

- [!] `golem [t-f5d33f] --provider codex --max-turns 35 "Run uv run pytest -q --tb=no 2>&1 | grep '^FAILED' | shuf | head -10. For each: run pytest on that file, read traceback, fix. Iterate until green. Commit."`

#### Build operon — Capco Week 1 toolkit (5 tasks)

- [ ] `golem [t-49cff5] --provider codex --max-turns 40 "Create effectors/stakeholder-map as a Python CLI. Maps stakeholders for a consulting engagement. Subcommands: add <name> --role <role> --influence <high|med|low> --disposition <champion|neutral|blocker>, list [--sort influence], matrix (prints influence/disposition grid), export (markdown). Stores in ~/.config/stakeholder-map/current.json. Commit."`
- [!] `golem [t-8b2f76] --provider infini --max-turns 40 "Create effectors/risk-register as a Python CLI for tracking risks on a consulting project. Subcommands: add <description> --likelihood <1-5> --impact <1-5> --owner <name> --mitigation <text>, list [--sort score], heatmap (ASCII 5x5 grid), export (markdown). Score = likelihood * impact. Stores in ~/.config/risk-register/risks.json. Commit."`

#### Build operon — organism capability (5 tasks)

- [!] `golem [t-ad66b7] --provider zhipu --max-turns 40 "Create effectors/golem-review as a Python script. Reads git log for golem: commits in last N hours (default 24). For each commit: shows files changed, runs pytest on changed test files, reports pass/fail. Summary: total commits, tests added, tests passing, tests failing. Usage: golem-review [--hours 24]. Commit."`
- [ ] `golem [t-332f9b] --provider codex --max-turns 40 "Create effectors/test-triage as a Python script. Runs pytest --tb=line, parses output, groups failures by error type (ImportError, FileNotFoundError, AssertionError, etc), reports: error_type | count | example_file. Useful for identifying patterns in mass golem output. Usage: test-triage [--fix-imports] to auto-fix common import errors. Commit."`
- [!] `golem [t-728a57] --provider zhipu --max-turns 35 "Create effectors/effector-health as a Python script. For each file in effectors/: check executable bit, try --help, check shebang, lint with ast.parse if Python. Report: name | executable | --help_ok | shebang | syntax. Fix trivially fixable issues (missing shebang, wrong permissions). Commit."`
- [!] `golem [t-cde59f] --provider infini --max-turns 35 "Create effectors/queue-analytics as a Python script. Reads loci/golem-queue.md. Reports: pending/done/failed counts, tasks by provider, avg task age, duplicate prompts, stale tasks (>24h pending). Usage: queue-analytics [--dedup] to remove duplicates. Commit."`
- [!] `golem [t-f0ad4e] --provider zhipu --max-turns 35 "Read metabolon/pathways/overnight.py. Improve to: 1) check daemon status before queuing, 2) estimate time to drain based on current provider throughput, 3) auto-clean completed tasks before queuing new ones. Run existing tests. Fix if broken. Commit."`

#### Daemon reliability (5 tasks)

- [!] `golem [t-75fc3e] --provider zhipu --max-turns 35 "Read effectors/golem-daemon. Add dedup logic: before dispatching a task, hash the prompt (minus task ID). If same hash already in-flight or completed in last 2h, skip. This prevents wasted slots on duplicate retries. Write tests. Commit."`
- [!] `golem [t-67e569] --provider codex --max-turns 35 "Read effectors/golem-daemon. Add a cmd_health subcommand that checks: 1) daemon PID alive, 2) each provider API reachable (quick test call), 3) disk space >1GB, 4) git status clean, 5) queue not stuck (tasks dispatched but no completions in >30min). Report as JSON. Commit."`
- [!] `golem [t-eb768f] --provider zhipu --max-turns 30 "Read effectors/golem-daemon. Find where failed tasks get retried. Add exponential backoff: 1st retry after 5min, 2nd after 15min, 3rd after 60min, then mark permanently failed. Currently retries may be too aggressive. Write tests. Commit."`
- [!] `golem [t-69c3b8] --provider infini --max-turns 30 "Read effectors/golem-daemon. Add a cmd_top subcommand that shows currently running golems with: task_id, provider, elapsed_time, estimated_remaining (based on --max-turns * avg time per turn). Like htop for golems. Commit."`
- [!] `golem [t-027122] --provider zhipu --max-turns 30 "Read effectors/golem. When a golem fails, append a one-line failure summary to loci/golem-failures.jsonl: {task_id, provider, error_type, error_msg, file, timestamp}. This enables post-hoc analysis without re-reading logs. Commit."`

### Mitogen wave 4 — Capco T-6 readiness + quality (2026-04-01 evening)

#### Consulting IP — Capco-ready frameworks (8 tasks, ZhiPu)

- [!] `golem [t-582384] --provider zhipu --max-turns 30 "Write a consulting insight card: AI operating model for banks — how to structure AI teams, CoE vs federated vs hybrid. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/ai-operating-model-banks.md. Structure: problem (2 sentences), why it matters (3 bullets), three model options with pros/cons, implementation roadmap, Capco angle. 600-900 words. Commit."`
- [!] `golem [t-e23cb0] --provider zhipu --max-turns 30 "Write a consulting insight card: AI POC-to-production playbook for financial services — why 87% of AI pilots fail to scale. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/ai-poc-to-production.md. Structure: problem, 5 failure modes, mitigation for each, production readiness checklist (10 items), Capco angle. 600-900 words. Commit."`
- [!] `golem [t-8a0215] --provider zhipu --max-turns 30 "Write a consulting insight card: Technology risk management for AI systems in banking — mapping AI risks to Basel/HKMA frameworks. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/tech-risk-mgmt-ai.md. Structure: risk taxonomy (model, data, operational, cyber, third-party), mapping to TM-E-1 and OR-1, control framework, Capco angle. 600-900 words. Commit."`
- [!] `golem [t-326b2c] --provider zhipu --max-turns 30 "Write a consulting insight card: Third-party AI risk assessment for banks — evaluating vendor AI solutions. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/third-party-ai-risk.md. Structure: problem, vendor assessment framework (8 dimensions), due diligence checklist, red flags, contract considerations, Capco angle. 600-900 words. Commit."`
- [!] `golem [t-7c35d2] --provider codex --max-turns 30 "Write a consulting insight card: AI talent strategy for banks — building vs buying AI capability. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/ai-talent-strategy.md. Structure: problem, three talent models, skills taxonomy for banking AI, retention strategies, upskilling pathways, Capco angle. 600-900 words. Commit."`
- [!] `golem [t-386ea9] --provider codex --max-turns 30 "Write a consulting insight card: Regulatory compliance checklist for AI deployment in HK banking — HKMA expectations mapped to implementation actions. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/hk-ai-compliance-checklist.md. Structure: 15-item checklist grouped by pre-deployment/deployment/post-deployment, each with: requirement, evidence needed, common gaps, remediation. Capco angle. 700-1000 words. Commit."`
- [!] `golem [t-cdb8cf] --provider codex --max-turns 30 "Write a consulting insight card: AI center of excellence setup guide for banks — from 0 to operating in 90 days. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/ai-coe-setup-guide.md. Structure: problem, 90-day roadmap (week-by-week), governance structure, team composition, success metrics, common pitfalls, Capco angle. 700-1000 words. Commit."`
- [!] `golem [t-1a3139] --provider zhipu --max-turns 30 "Write a consulting insight card: GenAI use case tiering for banks — a structured methodology to prioritize where LLMs add value vs where they introduce unacceptable risk. Write to ~/epigenome/chromatin/euchromatin/consulting/cards/genai-use-case-tiering.md. Structure: 4-tier model (autonomous/assisted/augmented/prohibited), classification criteria, example use cases per tier, governance requirements per tier, Capco angle. 600-900 words. Commit."`

#### Test coverage — untested metabolon modules (4 tasks)

- [!] `golem [t-3ee168] --provider zhipu --max-turns 40 "Write tests for /home/terry/germline/metabolon/organelles/golgi.py (339 lines). Read the module first. Mock external calls (subprocess, API, file I/O outside test dirs). Write to assays/test_organelles_golgi.py. Target 80%+ line coverage. Run uv run pytest assays/test_organelles_golgi.py -v. Fix all failures. Commit."`
- [!] `golem [t-733aca] --provider zhipu --max-turns 60 "Write tests for /home/terry/germline/metabolon/organelles/statolith.py (2049 lines — large module). Read the module first. Focus on public API functions. Mock external calls. Write to assays/test_organelles_statolith.py. This is a large module — prioritize critical paths over exhaustive coverage. Run uv run pytest assays/test_organelles_statolith.py -v. Fix all failures. Commit."`
- [!] `golem [t-6ddb89] --provider codex --max-turns 40 "Write tests for /home/terry/germline/metabolon/organelles/phenotype_translate.py (560 lines). Read the module first. Mock external calls. Write to assays/test_organelles_phenotype_translate.py. Target 80%+ line coverage. Run uv run pytest assays/test_organelles_phenotype_translate.py -v. Fix all failures. Commit."`
- [ ] `golem [t-8a7c3b] --provider codex --max-turns 25 "Write tests for /home/terry/germline/metabolon/enzymes/kinesin.py (63 lines — small module). Read the module first. Mock external calls. Write to assays/test_enzymes_kinesin.py. Aim for 100% coverage given small size. Run uv run pytest assays/test_enzymes_kinesin.py -v. Fix all failures. Commit."`

#### Fix operon — systematic test failures (4 tasks)

- [!] `golem [t-ee3dfa] --provider zhipu --max-turns 40 "Run uv run pytest --co -q 2>&1 | grep '^ERROR' | head -20. These are collection errors. For each: read the file, identify the import or syntax error, fix it. Common issues: hardcoded paths, missing modules, circular imports. Run --co again. Iterate until collection errors < 5. Commit."`
- [ ] `golem [t-7644e7] --provider zhipu --max-turns 40 "Run uv run pytest -x -q --tb=short 2>&1 | head -50. Find the FIRST failure. Read the test file and the source it tests. Fix the root cause (not just the test). Run pytest on that file until green. Move to the next failure. Fix at least 5 distinct failures. Commit."`
- [ ] `golem [t-dff086] --provider codex --max-turns 40 "Run uv run pytest -q --tb=no --continue-on-collection-errors 2>&1 | grep '^FAILED' | sed 's/::.*//g' | sort | uniq -c | sort -rn | head -3. For each of the top 3 files with most failures: run pytest on it alone, read full traceback, fix ALL failures in that file. Commit."`
- [ ] `golem [t-f11e90] --provider zhipu --max-turns 30 "Find assays/test_*.py files that import from effectors directly (import effectors.X or from effectors import). Effectors are scripts — they must be tested via subprocess.run, not imported. For each file found: rewrite the import-based tests to use subprocess. Run pytest on each changed file. Commit."`

#### Builds — Capco tooling (3 tasks)

- [ ] `golem [t-7fd981] --provider zhipu --max-turns 50 "Create effectors/consulting-timer as a Python CLI that tracks billable hours. Subcommands: start <client> [project], stop, status, report [--week|--month], log <client> <hours> <description>. Store in ~/.config/consulting-timer/log.jsonl. Format report as table: date | client | project | hours | description. Add argparse with --help. chmod +x. Write tests in assays/test_consulting_timer.py using subprocess. Commit."`
- [ ] `golem [t-51fa51] --provider codex --max-turns 50 "Create effectors/engagement-brief as a Python CLI that generates a structured brief for a new consulting engagement. Takes --client <name> --industry <industry> --scope <scope>. Outputs markdown with: client overview, key stakeholders section, engagement objectives, deliverables, timeline, risks, first-week priorities. Write to stdout. Add argparse with --help. chmod +x. Write tests using subprocess. Commit."`
- [ ] `golem [t-e33e24] --provider zhipu --max-turns 40 "Create effectors/standup-log as a Python CLI for daily standup notes. Subcommands: add <what-I-did> [--blocker <text>], show [--today|--week], export [--format md|json]. Store in ~/.config/standup/log.jsonl. Format output cleanly with dates and blockers highlighted. Add argparse. chmod +x. Write subprocess tests. Commit."`

#### Code quality (3 tasks)

- [ ] `golem [t-118cbf] --provider zhipu --max-turns 25 "Run: python3 -c \"import ast, sys, pathlib; [print(f) for f in sorted(pathlib.Path('assays').glob('test_*.py')) if not (lambda p: (ast.parse(p.read_text()), True)[1])(f)]\" 2>&1 | head -20. These are test files with syntax errors. For each: read the file, fix the syntax error. Run pytest on it. Commit."`
- [ ] `golem [t-7d80a8] --provider codex --max-turns 30 "Find effectors that crash on --help. Run: for f in effectors/*; do [ -x \"\$f\" ] && timeout 5 \"\$f\" --help >/dev/null 2>&1 || echo \"CRASH: \$f\"; done. For each crasher: read the file, add proper argparse or fix the crash. Commit."`
- [ ] `golem [t-bb1417] --provider zhipu --max-turns 25 "Check metabolon/**/*.py for unused imports. Run: python3 -c \"import ast, pathlib; ...\". For files with >3 unused imports, remove them. Run pytest on changed files to verify no breakage. Commit."`
