# Task 6: MCP Tool Usage

Search the web for the latest stable version number of the Python package `uv` (astral-sh/uv).
Write a file called `answer.json` with this exact structure:

```json
{
  "package": "uv",
  "version": "<version string>",
  "source": "<URL you found it from>"
}
```

You MUST use web search to find the current version — do not guess from training data.
