# Task 9: Coaching-Constrained Implementation

Read COACHING.md for mandatory constraints, then implement the task below.

## Task
Write `fetcher.py` — a function `fetch_json(url: str) -> dict` that:
- Fetches JSON from a URL
- Returns the parsed dict
- Handles network errors and invalid JSON gracefully

Write `test_fetcher.py` with at least 5 tests. Run pytest.
